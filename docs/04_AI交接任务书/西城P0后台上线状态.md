# 西城 P0 后台上线状态

更新时间：2026-07-03
分支：`feature/xicheng-p0`

## 当前结论

2026-07-03 03:26 CST 已重新同步并核验当前分支：本地 `HEAD`、Gitee `origin/feature/xicheng-p0`、GitHub `github/feature/xicheng-p0` 均为 `87c31068ed9d647bfdbe48f428afdb91d049aefd`，工作树干净。该提交已包含文旅后台文本知识库和图片素材库迁移结果：运营端已有文旅资料文件上传与图片素材文件上传入口，后端已有 `/knowledge-documents/upload`、`/media-assets/upload`，上传路径使用 `xunjing/tourism-knowledge` 与 `xunjing/tourism-media`，并继续落在现有 Xunjing/Yudao MySQL 表、Yudao `FileApi` 对象存储和现有知识库关联体系内；本轮没有引入学校或资源包新范围。

2026-07-03 03:23 CST 最新公网 Yudao server smoke 已通过：`qa/xicheng-yudao-server-smoke-evidence.json` 输出 `XICHENG_YUDAO_SERVER_SMOKE_READY`，`baseUrl=https://xunjingapi.xingheai.net`，`packageCode=XICHENG-MAP-001`，`packageRegionCode=beijing-xicheng`，`mediaAssetCount=8`、`publicReportReviewedKnowledgeCount=80`、`publicReportReviewedMediaCount=8`、`publicReportMapPointCount=80`；build evidence 绑定同一 commit `87c31068ed9d647bfdbe48f428afdb91d049aefd`，JAR SHA-256 为 `4fed65d9ac7cf120a2f3e6174bceb33bc83739d2fef5fbe1126d92b3791cc752`。

2026-07-03 03:26 CST 最新 `npm run xunjing:yudao:release:preflight` 仍为 `NOT_READY`，但阻塞已收敛到正式微信生产配置：失败检查只剩 `runtime-env` 与 `real-wechat-app`，`blockerCount=4`、owner-lane task CSV 为 6 行，POI task CSV 为 0 行。`appReadinessStatus=READY`、`productionReviewTasksStatus=PRODUCTION_REVIEW_APPLIED`、`productionReviewPendingPoiCount=0`，说明 80 个西城 POI 的生产复核已不再是当前上线阻塞。正式 production 仍必须补齐真实 `WX_MP_APP_ID`、`WX_MP_SECRET`、`WX_MINIAPP_SECRET` 并重新跑 release gate；当前 `tmp/xunjing-platform-server.env` 里这三项仍被判定为占位，不能用来发布小程序生产版。

2026-07-02 15:30 本轮范围已收窄为“只做后台和 H5/API 试运行”。新增门禁阶段 `api-trial`：它继续要求真实 HTTPS API、Yudao 后端构建和公网 smoke、Admin UI 构建物、80 个西城生产 POI、来源/生产复核、production seed apply、运行库 seed 复验、AI/Qdrant/Embedding/OCR/对象存储证据和 H5/API live readiness；但不把微信 MP/小程序 AppID/Secret 作为本轮试运行阻塞。正式小程序上线仍必须使用 `--stage production`，并继续通过 `runtime-env` 和 `real-wechat-app` 检查真实微信配置。`api-trial` 通过时只能称为 `API_TRIAL_READY_CANDIDATE`，不能称为 `PRODUCTION_READY_CANDIDATE`。

后台 + H5/API 试运行验收命令使用：

```bash
npm run xunjing:yudao:release:gate -- \
  --stage api-trial \
  --expected-branch feature/xicheng-p0 \
  --env-file tmp/xunjing-platform-server.env \
  --yudao-server-build-evidence qa/xicheng-yudao-server-build-evidence.json \
  --yudao-server-smoke-evidence qa/xicheng-yudao-server-smoke-evidence.json \
  --ai-bootstrap-evidence qa/xicheng-yudao-ai-bootstrap-evidence.json \
  --embedding-evidence qa/xicheng-embedding-smoke-evidence.json \
  --qdrant-evidence qa/xicheng-qdrant-smoke-evidence.json \
  --vision-ocr-evidence qa/xicheng-vision-ocr-smoke-evidence.json \
  --object-storage-evidence qa/xicheng-object-storage-smoke-evidence.json \
  --runtime-seed-evidence qa/xicheng-yudao-runtime-seed-production-evidence.json \
  --production-seed-apply-evidence qa/xicheng-yudao-production-seed-apply-evidence.json \
  --poi-manifest-evidence qa/xicheng-poi-manifest-evidence.json \
  --poi-workbook-evidence qa/xicheng-poi-review-workbook-evidence.json \
  --poi-seed-evidence qa/xicheng-poi-production-seed-evidence.json \
  --evidence-file qa/xicheng-yudao-api-trial-release-evidence.json
```

生成 H5/API live readiness 后，再用 `npm run xunjing:xicheng:release:evidence:package -- --stage api-trial ... --app-readiness-evidence qa/xicheng-app-readiness-evidence.json --evidence-file qa/xicheng-api-trial-release-evidence-package.json` 打最终试运行证据包。正式生产证据包仍用 `--stage production`，并且必须先补齐真实微信配置。

状态：公网 Yudao 后端候选和运营端候选已部署，后端可通过 `https://xunjingapi.xingheai.net` 访问，运营端 `https://xunjingadmin.xingheai.net` 已开启 HTTPS + Basic 门禁；整体仍为 `NOT_READY` for 生产上线。

2026-07-02 15:58 新增并复验 `npm run xunjing:xicheng:h5-api:trial`，独立生成 `qa/xicheng-h5-api-trial-evidence.json`：`status=XICHENG_H5_API_TRIAL_READY`，`apiBaseUrl=https://xunjingapi.xingheai.net`，`adminBaseUrl=https://xunjingadmin.xingheai.net`，`publicReportP0Ready=true`，`reviewedKnowledgeCount=80`、`reviewedMediaCount=8`、`mapPointCount=80`，运营端返回 `adminH5Mode=BASIC_AUTH_PROTECTED`、`adminBasicAuthRealm="Xinghe Xunjing Admin"`。这只证明后台 + H5/API 试运行域名和数据链路可用，不替代正式小程序 production 所需的真实微信 `WX_MP_APP_ID`、`WX_MP_SECRET`、`WX_MINIAPP_SECRET`。

2026-07-02 14:13 已把 `github/main` 的西城 App/后台触发链路更新合入 `feature/xicheng-p0`，并已推送到 Gitee `origin/feature/xicheng-p0` 和 GitHub `github/feature/xicheng-p0`。本轮每次文档提交后都必须重新生成 `qa/xicheng-yudao-server-build-evidence.json`、重新部署服务器 release、再生成 `qa/xicheng-yudao-server-smoke-evidence.json`，确保 build evidence、server smoke evidence 与当前 Git HEAD 一致；具体 Git commit、JAR SHA-256 和服务器 current release 以当次 evidence 和 `readlink -f /opt/xinghe-xunjing/backend/yudao/current` 为准，不能复用旧提交的 evidence。

2026-07-02 14:10 已重新确认 `xunjingadmin.xingheai.net` 返回 HTTP 401 Basic 门禁，服务器 `/opt/xinghe-xunjing/admin/current` 指向真实 Yudao Admin SPA release `/opt/xinghe-xunjing/admin/releases/20260702-094517-f37cbbee`，包含 `index.html`、Vue `#app` 挂载和 1747 个静态文件。`npm run xunjing:yudao:release:gate` 的 `xunjing-admin-ui-artifact` 门禁已通过，默认检查 `backend/yudao/yudao-ui/yudao-ui-admin-vue3/dist`，生产部署也可用 `--admin-ui-dir` 或 `XUNJING_ADMIN_UI_DIR` 指向服务器实际构建目录；占位页、缺 `index.html`、缺 Vue `#app` 挂载或缺 JS bundle 都会 fail-closed。

2026-07-02 14:13 复跑 `npm run xunjing:yudao:release:gate` 后仍输出 `NOT_READY`，但失败项只剩 `runtime-env` 和 `real-wechat-app`：`WX_MP_APP_ID`、`WX_MP_SECRET`、`WX_MINIAPP_SECRET` 仍是缺失或占位。Qdrant、Embedding、视觉/OCR、对象存储、Yudao AI 默认模型、完整 Yudao baseline、可部署 JAR、server smoke、Admin UI、80 个生产 POI、来源覆盖、来源回填、生产复核回填、production seed apply 和运行库 seed 复验均已通过当前 gate。

2026-07-01 已将 `feature/xicheng-p0` 同步到 Gitee 与 GitHub。本轮先在后端修复提交 `e9336b9cfafd62fd915e98e1573398a8170d500c` 解决 `/app-api/xunjing/resource/package?packageCode=XICHENG-MAP-001` 缺少 `regionCode` 的问题，随后基于最新分支重新构建并部署 `yudao-server.jar` 到 `101.200.137.138`；当前 release 的 Git commit、jar SHA-256 和 evidence 时间以 `qa/xicheng-yudao-release-evidence.json`、`qa/xicheng-yudao-server-build-evidence.json` 与 `qa/xicheng-yudao-server-smoke-evidence.json` 为准。公网 smoke 已通过：`qa/xicheng-yudao-server-smoke-evidence.json` 为 `XICHENG_YUDAO_SERVER_SMOKE_READY`，`baseUrl=https://xunjingapi.xingheai.net`，`packageRegionCode=beijing-xicheng`，公开报告计数为 `packageCount=1`、`reviewedKnowledgeCount=84`、`mapPointCount=80`。APP live readiness 也已用同一 HTTPS 后端通过 20/20，包含扫码解析、错误反馈、AI 有来源回答、AI 无来源拦截，以及白塔寺/恭王府/北京天文馆 trigger smoke。

这仍不能判定为生产上线。2026-07-01 最新 `npm run xunjing:yudao:release:preflight` 仍输出 `NOT_READY`：已刷新官方来源覆盖和来源审核回填，`SOURCE_COVERAGE_READY` 覆盖 25 组来源、80/80 POI，`SOURCE_REVIEW_APPLIED` 回填 80/80、pending=0；但 `qa/xicheng-poi-production-review-apply-evidence.json` 仍为 `PRODUCTION_REVIEW_DATA_REMAINS`，`approvedReviewRowCount=0`、`pendingProductionReviewPoiCount=80`。当前真正阻塞是：生产 env 尚无真实微信 AppID/Secret、Qwen/DashScope、Qdrant、OCR/视觉、OSS、内部鉴权等密钥和 smoke evidence；80 个西城 POI 仍缺实地/坐标/内容/授权生产复核；生产 seed、runtime seed、AI bootstrap、Qdrant、Embedding、OCR、对象存储和最终 release package evidence 仍未通过。

2026-06-30 12:11 在 `feature/xicheng-p0` 复跑 `node scripts/run-xicheng-yudao-release-preflight.mjs`，当前仍为 `NOT_READY`：`summary.failedCheckCount=17`、`summary.blockerCount=51`、`summary.taskCount=68`、`summary.poiTaskCount=640`；APP readiness 为 `MISSING`，生产复核任务为 `PRODUCTION_REVIEW_TASKS_REQUIRED`，仍有 80 个 POI、800 个 production review 字段任务待完成。当前 blocker 只接受真实证据：`qa/xicheng-app-readiness-evidence.json` 必须由非本地 HTTPS 后端生成并与 release evidence 的 `summary.appApiBaseUrl` 一致；`qa/xicheng-app-readiness-local-evidence.json` 只证明本地 HTTP 20/20，不得改名或复制成生产 APP readiness evidence。POI production review 必须由 `workbench/xicheng-poi-production-review-summary.csv` 填入真实 `photoEvidenceStatus`、`fieldEvidenceRefs`、`fieldVerifiedBy/fieldVerifiedAt`、`reviewStatus`、`geoStatus`、`auditLicenseStatus`、`status` 等字段后再跑 `production-review:apply`，不能用脚本自动假填。

2026-06-30 本轮提交 `e1f8cf1d fix(app): route legacy index to xicheng by default` 已修复 H5 直接打开历史兼容入口 `#/pages/index/index` 时误显示喀什首页的问题：无显式喀什参数会跳转到 `#/pages/xicheng/home/home`，带 `city=kashgar`、`mode=play/landing`、`dramaId` 或扫码场景时继续保留喀什兼容。浏览器实测默认旧入口进入西城，显式喀什入口仍保留；APP 全量 `tests/*.test.mjs`、APP `npm run build`、根目录 `npm run test:run` 和 `npm run xunjing:platform:verify:static` 均通过。尝试用当前本地 `http://127.0.0.1:48080` 刷新 `qa/xicheng-app-readiness-local-evidence.json` 未通过，因为该端口当前运行的是另一个 Yudao 实例并对资源包接口返回 401；不能把这次失败或旧本地 HTTP evidence 当成生产 APP readiness。

2026-06-29 当前协作分支为 `feature/xicheng-p0`，每轮接手先用 `git rev-list --left-right --count HEAD...origin/feature/xicheng-p0` 和 `HEAD...github/feature/xicheng-p0` 确认 Gitee/GitHub 同步。最新 production preflight 仍是 `NOT_READY`，以当次 stdout 的 `summary.failedCheckCount`、`summary.blockerCount`、`summary.taskCount`、`summary.poiTaskCount` 和 `summary.poiSummaryCount` 为准；这不是本地接口不可用，而是生产外部资源与 POI 生产审核证据尚未齐全。当前 source coverage live evidence 已证明 25 个来源组覆盖 80/80 POI，source-review apply evidence 已为 `SOURCE_REVIEW_APPLIED`、80/80 已回填、pending=0；新增 `npm run xunjing:xicheng:poi:source-review:prepare-official` 可在同一 source review CSV 上预填 `OFFICIAL_PUBLIC` 来源组授权字段，再由 `source-review:apply` 生成 source-applied workbook evidence。2026-06-29 本地 `http://127.0.0.1:48080` 对 80 条 source-applied POI 运行真实 trigger smoke：先暴露 `xicheng-yandai-xiejie` 被旧库 `xicheng-shichahai` 别名污染误识别，随后对本地 `xunjing_poi` 做最小数据修正，移除 `xicheng-shichahai.aliases_json` 中的“烟袋斜街”，重跑后 80/80 通过，trigger smoke evidence 为 `XICHENG_POI_TRIGGER_SMOKE_READY`，trigger smoke apply evidence 为 `TRIGGER_SMOKE_APPLIED`、pending=0。本分支也已修复烟袋斜街与什刹海的别名/同分排序冲突，并同步修正 seed SQL；其它环境必须重灌最新 seed 或执行等价数据修正后再重跑 trigger smoke。review pack 仍只用于补齐真实资料，不能当 production evidence。

本轮已把西城后台从“只有 Java 内置 5 个 POI 样例”推进到“有可初始化、可门禁校验、可 fresh local 启动联调的 80 条西城 POI/来源/评测 seed，并且 `/app-api/xunjing/triggers/resolve` 可优先读取 `xunjing_poi` 表”。2026-06-28 已在本机 `xunjing-mysql` 的 `yudao_xinghe_xunjing` 库重新执行 `backend/yudao/sql/mysql/xunjing-seed-xicheng-p0.sql`，确认 `XICHENG-MAP-001` 已发布、`xunjing_poi` 有 80 条北京西城真实候选 POI、`xunjing_map_point` 有 80 条点位、`xunjing_knowledge_document` 有 84 条西城来源文档，报告 `p0LocalCandidate=true`、`productionReady=false`、`poiSeedCount=80`；APP API 调用必须带 `tenant-id: 1`，本机已复验 `/app-api/xunjing/scan/resolve` 可解析 `QR-XICHENG-MAP-001`，`/app-api/xunjing/triggers/resolve` 可命中 `xicheng-baitasi`，`/app-api/xunjing/public-report/summary?packageCode=XICHENG-MAP-001` 返回 `packageCount=1`、`reviewedKnowledgeCount=84`、`mapPointCount=80`、`qrCodeCount=1`，`/app-api/xunjing/ai/chat` 可引用“妙应寺白塔 POI 级已审核来源”并记录 `xunjing_ai_generation_log`。触发识别侧已补 `TRIGGER_RESOLVE` 事件：APP 请求带 `packageCode` 时，后台会把识别结果和脱敏上下文写入 `xunjing_interaction_event`。APP 事件侧已补 `ERROR_FEEDBACK` 类型，支持西城 APP 用普通场景码上报识别失败、AI 反馈或素材异常。AI 对话侧已补 POI 来源约束：具体 POI 问答不能使用其他 POI 或泛化西城资料冒充来源，未命中匹配来源时返回 `BLOCKED`。

2026-06-28 新增 `npm run xunjing:yudao:runtime-seed:verify`，用于直接查询当前 Yudao MySQL 运行库并生成脱敏证据，避免只看 SQL 文件就误判“后台已填充”。本机可用命令示例：`MYSQL_CLIENT=container MYSQL_CONTAINER=xunjing-mysql npm run xunjing:yudao:runtime-seed:verify -- --env-file ops/xunjing-platform.env.example --evidence-file qa/xicheng-yudao-runtime-seed-evidence.json`。该证据只证明本地候选 seed 已真实落库；只要 `geo_status` 或 `license_status` 仍为 `REVIEW_REQUIRED`，就继续输出 `productionReady=false`，不能替代 production POI manifest/seed evidence。预发/生产库必须追加 `--mode production` 或 `--stage production`，只有输出 `YUDAO_XICHENG_PRODUCTION_SEED_READY` 才能证明生产运行库已经应用已复核 POI seed。

2026-06-29 新增 `backend/yudao/sql/mysql/xunjing-patch-xicheng-trigger-alias-20260629.sql` 和 `npm run xunjing:yudao:trigger-alias:verify`，把本机手动修正烟袋斜街/什刹海别名冲突的动作变成可审查、可复跑的运行库步骤。旧环境若仍把“烟袋斜街”保留在 `xicheng-shichahai.aliases_json`，先审查并执行该 patch，再用 `MYSQL_CLIENT=container MYSQL_CONTAINER=xunjing-mysql npm run xunjing:yudao:trigger-alias:verify -- --env-file ops/xunjing-platform.env.example --evidence-file qa/xicheng-yudao-trigger-alias-evidence.json` 复验；只有输出 `YUDAO_XICHENG_TRIGGER_ALIAS_READY` 后再跑 80 POI trigger smoke。`npm run xunjing:yudao:runtime-seed:verify` 已同步纳入 `shichahaiHasYandaiAlias=0`、`yandaiHasAlias=1` 和 `yandaiHasTriggerKeyword=1` 检查，后续生产 runtime seed evidence 不能绕过该识别冲突。

2026-06-28 已用 `backend/yudao/yudao-server/target/yudao-server.jar` 通过 Docker JRE 复测本地接口：按 `ops/xunjing-platform.env.example` 明确设置 `SPRING_AI_VECTORSTORE_TYPE=none`、`SPRING_AI_MODEL_EMBEDDING=none`、`DASHSCOPE_EMBEDDING_ENABLED=false` 后，Yudao Server 可在 `http://127.0.0.1:48080` 启动并返回真实 APP API。`/app-api/xunjing/resource/package?packageCode=XICHENG-MAP-001` 返回 80 个 map point 和 84 条 knowledge document；`/app-api/xunjing/triggers/resolve` 可用 OCR 文本“妙应寺白塔”命中 `xicheng-baitasi`，返回 `confidence=0.99`、`requiresUserConfirm=false` 和带 `packageCode=XICHENG-MAP-001` 的 AI 讲解路径。`node scripts/verify-xunjing-platform-readiness.mjs --env-file ops/xunjing-platform.env.example --base-url http://127.0.0.1:48080 --tenant-id 1 --skip-admin-check --include-xicheng-app-check --include-xicheng-trigger-check --evidence-file tmp/xicheng-app-readiness-evidence-local.json` 已通过 20/20 项本地真实 HTTP 检查。

当前分支已纳入完整且已脱敏的 Yudao baseline `backend/yudao/sql/mysql/ruoyi-vue-pro.sql`，本地依赖栈仍保留 `xunjing-yudao-runtime-minimal.sql` 作为 local-candidate App API readiness 兜底。新增的 `npm run xunjing:yudao:release:gate` 会把完整 Yudao baseline、HTTPS APP API 域名、微信 AppID、AI/OCR/视觉服务、Qdrant collection smoke evidence、对象存储、80 个以上西城已复核 POI、来源授权和 Yudao 运行库 production seed evidence 作为生产 fail-closed 门禁；baseline 中混入未脱敏短信、邮件或文件存储 provider 凭证也会被拦截。release evidence 会写入 `runtimeEnvFingerprintMode=redacted-runtime-env-v1`、`runtimeEnvNonSecretSha256` 和 `runtimeEnvSecretPresenceSha256`，只记录非密钥配置哈希和密钥存在状态，不落地真实密钥值。这还不是生产上线：当前 seed 虽已有 80 个本地候选点位，但坐标、来源授权和现场识别证据仍标记为 `REVIEW_REQUIRED`；生产或预发不能使用本地 `none` 向量配置，仍必须配置真实 Embedding provider、Qdrant、OCR/视觉、对象存储、HTTPS 域名和微信 AppID，并把生产 seed 导入目标 Yudao 库后跑出 `YUDAO_XICHENG_PRODUCTION_SEED_READY` 证据。

生产环境变量从 `ops/xicheng-production.env.example` 复制到安全路径（例如 `/secure/path/production.env`）后填写真实值；真实 env 文件和密钥不得提交 Git。该模板覆盖 release gate 所需 MySQL、Redis、Qdrant、DashScope/Qwen、微信、OCR/视觉、对象存储、HTTPS APP API 域名和内部鉴权变量；`QDRANT_API_KEY`、`OSS_REGION`、`OSS_PATH_STYLE` 也是生产门禁必填项。

## 已完成后台能力

- 新增 `xunjing_poi` 表结构，承载 `poi_code`、`region_code`、别名 JSON、触发 JSON、来源 JSON、内容审核、坐标审核和授权状态。
- 新增 `backend/yudao/sql/mysql/xunjing-seed-xicheng-p0.sql`。
- 西城 seed 初始化 `XICHENG-2026-P0`、`XICHENG-MAP-001`、`QR-XICHENG-MAP-001`。
- 当前 80 个西城本地候选 POI 覆盖白塔寺、历代帝王庙、北海公园、什刹海、大栅栏、恭王府、北京动物园、北京天文馆、北京大观园、宣武艺园、金中都公园、中国地质博物馆、宣南文化博物馆、宋庆龄故居、郭沫若纪念馆、鲁迅博物馆、湖广会馆大戏楼、老舍茶馆、阜成门内大街、烟袋斜街、银锭桥、牛街、金融街，以及来自西城区文物保护单位公开名单的文保候选点。
- seed 同步写入 `xunjing_map_point`，保证现有 APP 资源包地图接口仍可读。
- seed 写入 4 条西城通用已审核知识文档，并从 80 个 `xunjing_poi` 自动生成 80 条 POI 级已审核来源文档；`/app-api/xunjing/ai/chat` 的白塔寺 smoke 已可返回匹配 `xicheng-baitasi` 的 sources。
- seed 写入西城 AI 固定评测集、评测 case、配额规则和本地候选状态报告。
- `XunjingMultimodalTriggerEngine` 已优先从 `xunjing_poi` 查询 `status=PUBLISHED`、`review_status=APPROVED` 的西城 POI，解析 `aliases_json`、`trigger_json`、`content_json`、`source_json` 后参与 OCR、定位、文本、图片和上下文打分；APP 请求带 `packageCode` 时，POI 候选会限定在该公开资源包的 `package_id` 内，避免同一区域不同产品包之间串包识别。
- `/app-api/xunjing/triggers/resolve` 请求带 `packageCode` 时，会自动记录 `TRIGGER_RESOLVE` 事件到 `xunjing_interaction_event`；事件 payload 包含 `sceneCode`、`regionCode`、`poiCode`、`poiName`、`confidence`、`requiresUserConfirm`、OCR 文本、图片标签数量、定位摘要、候选数量和来源数量，不写入 `imageBase64` 或 `imageUrl`。
- `/app-api/xunjing/resource/events` 已允许 `ERROR_FEEDBACK` 事件；当请求带 `packageCode` 时，`sceneCode` 可使用普通 APP 场景码，不再强制必须是二维码场景码。
- 当数据库 POI 为空或查询异常时，trigger 引擎仍保留 Java 内置 5 个 POI fallback，避免本地空库场景直接不可用。
- 纳入完整 Yudao baseline `backend/yudao/sql/mysql/ruoyi-vue-pro.sql`，并已脱敏上游示例短信、邮件、文件存储凭证；`backend/yudao/sql/mysql/xunjing-yudao-runtime-minimal.sql` 仍作为本地候选 fresh DB 的最小兜底，只创建 `system_tenant`、`infra_api_access_log`、`infra_api_error_log` 和租户 1。
- `ops/mysql-init/xunjing-init.sh` 已导入 minimal Yudao runtime、AI 表、寻境 schema、西城 seed，并校验 `XICHENG-MAP-001`、80 个西城已审核本地候选 POI、80 条 POI 级已审核来源文档。
- `/app-api/xunjing/ai/chat` 带 `regionCode`、`poiCode`、`poiName` 时，已要求 `sources` 与该 POI 匹配；只有其他 POI 或泛化资料时返回 `BLOCKED`，并记录 source guard 日志。
- `/app-api/xunjing/ai/chat` 的西城 `xicheng-rag-chat` 场景已修复 quota 规则开启时的 500 问题；当前通过数据库聚合统计 quota 用量并显式带 `tenant_id`，`xicheng-rag-chat` 与 APP 主场景 `xicheng-ai-guide` 均可返回匹配 POI 来源的 `PASSED` 响应。
- `npm run xunjing:platform:verify:static` 已新增 `xicheng-seed-data`、`xicheng-poi-seed-quality`、`xicheng-trigger-backend` 与 `xicheng-ai-source-guard-backend` 静态门禁。
- live readiness 已支持 `--include-xicheng-trigger-check`，可对 `/app-api/xunjing/triggers/resolve` 执行 `xicheng-baitasi`、`xicheng-gongwangfu`、`xicheng-planetarium` 三个西城 DB seed POI 的真实 HTTP smoke。
- live readiness 已支持并在 fresh local stack 通过 `--include-xicheng-app-check --include-xicheng-trigger-check`，可对 `QR-XICHENG-MAP-001` 扫码、`ERROR_FEEDBACK` 写入、`/app-api/xunjing/ai/chat` 西城匹配来源 `PASSED`、无匹配来源 `BLOCKED`、以及三个西城 trigger smoke 生成真实 HTTP evidence。
- 新增 `scripts/verify-xicheng-yudao-release-readiness.mjs` 和 `npm run xunjing:yudao:release:gate`，用于生产/预发前 fail-closed 检查；未满足真实外部资源、Yudao AI 默认模型 bootstrap evidence、可部署 `yudao-server.jar`、真实 Yudao Admin 静态构建物、80+ 已复核 POI、manifest evidence 和 seed SQL evidence 时输出 `NOT_READY`，满足后才输出 `PRODUCTION_READY_CANDIDATE` 或 `PREPROD_READY_CANDIDATE`。当前分支的 `full-yudao-baseline` 检查已可通过，并会拒绝 baseline 中未脱敏的 provider 凭证。
- 新增 `scripts/verify-xicheng-qdrant-smoke.mjs` 和 `npm run xunjing:qdrant:smoke`，用于对真实 Qdrant REST API 查询文本与图片 collection；证据状态必须为 `XICHENG_QDRANT_SMOKE_READY`，只记录 provider host、`/collections` endpoint、collection 名称、HTTP 状态、collection 状态、points count 和耗时，不保存 `QDRANT_API_KEY`。release gate 通过 `--qdrant-evidence qa/xicheng-qdrant-smoke-evidence.json` 或 `XICHENG_QDRANT_EVIDENCE` 校验；只配置 `QDRANT_URL`、`QDRANT_API_KEY`、`QDRANT_TEXT_COLLECTION`、`QDRANT_IMAGE_COLLECTION` 不能作为生产向量库证据。
- 新增 `scripts/verify-xicheng-embedding-smoke.mjs` 和 `npm run xunjing:embedding:smoke`，用于对真实 Embedding provider 执行 `/embeddings` smoke；证据状态必须为 `XICHENG_EMBEDDING_SMOKE_READY`，只记录 host、endpoint path、model、HTTP 状态、向量维度和有限数值数量，不保存 `DASHSCOPE_API_KEY`、请求文本或向量值。release gate 通过 `--embedding-evidence qa/xicheng-embedding-smoke-evidence.json` 或 `XICHENG_EMBEDDING_EVIDENCE` 校验；只配置 `SPRING_AI_MODEL_EMBEDDING`、`DASHSCOPE_EMBEDDING_ENABLED` 和 `QWEN_EMBEDDING_MODEL` 不能作为生产 Embedding 可用证据。
- 新增 `scripts/verify-xicheng-vision-ocr-smoke.mjs` 和 `npm run xunjing:vision:smoke`，用于对真实 OCR/视觉 provider 执行 OpenAI-compatible multimodal `/chat/completions` smoke；证据状态必须为 `XICHENG_VISION_OCR_SMOKE_READY`，只记录 host、endpoint path、model、HTTP 状态、响应长度、labels、sample image 引用和耗时，不保存 API key、请求体或完整回答。release gate 通过 `--vision-ocr-evidence qa/xicheng-vision-ocr-smoke-evidence.json` 或 `XICHENG_VISION_OCR_EVIDENCE` 校验；只配置 `XUNJING_VISION_API_URL`、`XUNJING_VISION_API_KEY`、`XUNJING_VISION_MODEL` 不能作为生产 OCR 证据。
- 新增 `scripts/verify-xicheng-object-storage-smoke.mjs` 和 `npm run xunjing:storage:smoke`，用于对真实对象存储执行 S3-compatible PUT/GET/DELETE smoke；证据状态必须为 `XICHENG_OBJECT_STORAGE_SMOKE_READY`，只记录 endpoint host、request host、bucket、prefix、object key SHA-256、region、pathStyle、PUT/GET/DELETE HTTP 状态、读回匹配和删除状态，不保存 access key、secret 或上传正文。release gate 将通过 `--object-storage-evidence qa/xicheng-object-storage-smoke-evidence.json` 或 `XICHENG_OBJECT_STORAGE_EVIDENCE` 校验，且要求 evidence 的 `region`、`pathStyle` 与生产 env 的 `OSS_REGION`、`OSS_PATH_STYLE` 一致；只配置对象存储 env 不能作为生产上传凭证证据。
- 新增 `scripts/create-xicheng-poi-production-manifest-template.mjs` 和 `npm run xunjing:xicheng:poi:manifest:template`，用于生成 `workbench/xicheng-production-pois.template.json` 字段模板；模板默认 80 个 POI 槽位、`productionReady=false`，并带根级 `reviewBatch` 占位字段。也可用 `--seed-sql backend/yudao/sql/mysql/xunjing-seed-xicheng-p0.sql` 把当前 80 个本地候选 POI 预填进草稿；配合 `--review-checklist workbench/xicheng-production-pois.review-checklist.csv` 可输出逐点审核清单，列出每个 POI 待补的授权、坐标、现场证据和审核字段；配合 `--review-workbook workbench/xicheng-production-pois.review-workbook.csv` 可输出可填写的生产 POI 字段表，数组字段用 `|` 分隔，方便审核人补齐真实资料后回填 manifest；配合 `--review-packet workbench/xicheng-production-pois.review-packet.json` 可输出固定下一步命令、`npm run xunjing:xicheng:poi:tasks:export` 任务导出命令和 evidence 路径交接包。预填草稿、两份 CSV、任务 CSV 和 review packet 仍保持审核、授权和现场证据为待复核状态，不会通过 production manifest gate，不能作为 production evidence，必须填入真实审核资料后才能作为 `workbench/xicheng-production-pois.json` 运行门禁。
- seed 预填模板已按每个 POI 行的 `source_json`/`content_json` 保留实际官方来源 URL；例如 `xicheng-heritage-001-sanguanmiao` 这类文保点位会进入 `西城区文物保护单位（81处）` 来源，而不会被统一写成 A 级景区目录，避免 production review workbook 指向错误来源。
- 新增 `scripts/create-xicheng-poi-production-review-pack.mjs` 和 `npm run xunjing:xicheng:poi:review:pack`，用于一条命令生成默认西城 POI 审核包：`workbench/xicheng-production-pois.prefilled.json`、`workbench/xicheng-production-pois.review-checklist.csv`、`workbench/xicheng-production-pois.review-workbook.csv`、`workbench/xicheng-production-pois.review-packet.json`、`qa/xicheng-poi-production-review-pack-evidence.json`、`qa/xicheng-poi-review-workbook-evidence.json` 和 `workbench/xicheng-poi-review-tasks.csv`。该命令固定输出 `REVIEW_DATA_REQUIRED`，并在 summary 中返回 `reviewPackEvidenceFile`、`workbookGateStatus`、`workbookReadyPoiCount`、`workbookPendingPoiCount`、`reviewTaskStatus`、`reviewTaskCount`、`reviewTaskOwnerLaneCounts` 和 `reviewTaskOwnerLaneBreakdown`，只用于按负责人、POI 清单、阻塞组和字段列把真实资料补齐任务交给人工或其它 AI，不是 production evidence。
- `npm run xunjing:xicheng:poi:workbook:gate` 的 evidence summary 已包含 `workbookReadyPoiCount`、`workbookPendingPoiCount`、`pendingPoiCodes`、`pendingPoiTasks` 和 `blockerBreakdown`，既能看到行级已完成/待补 POI 数，也能用 `pendingPoiTasks[].workbookRowNumber`、`pendingPoiTasks[].poiCode`、`pendingPoiTasks[].blockerGroups` 把单个 POI 行拆到资料、授权、现场核验、内容审核或占位清理负责人，并按 `poi-identity`、`poi-source-license`、`poi-field-evidence`、`poi-content-audit`、`no-placeholder-cells` 等检查项统计整体 blocker 数。
- 新增 `scripts/export-xicheng-poi-review-tasks.mjs` 和 `npm run xunjing:xicheng:poi:tasks:export`，用于把 workbook gate 的 `summary.pendingPoiTasks` 展开为 `workbench/xicheng-poi-review-tasks.csv`；CSV 按 `workbookRowNumber`、`poiCode`、`blockerGroup`、`ownerLane` 输出一行一个补齐任务，并带 `taskDetail`、`requiredEvidence`、`workbookColumns`，方便把 80 个 POI 的资料补录、来源授权、现场核验、内容审核和占位清理拆给不同负责人。脚本 stdout 的 `summary.ownerLaneBreakdown` 会按负责人输出 `taskCount`、`poiCount`、`poiCodes`、`blockerGroups` 和 `workbookColumns`，供另一个 AI 先按 lane 认领任务范围。该 CSV 是协作任务单，不是 production evidence。
- 新增 `scripts/export-xicheng-poi-production-review-tasks.mjs` 和 `npm run xunjing:xicheng:poi:production-review:tasks:export`，用于把 `workbench/xicheng-poi-production-review-summary.csv` 中逐 POI 现场证据、触发烟测、坐标审核、内容审核和发布字段缺口展开为 `workbench/xicheng-poi-production-review-tasks.csv`，同时写入 `workbench/xicheng-poi-production-review-owner-lanes.csv` 和 `qa/xicheng-poi-production-review-tasks-evidence.json`。当仍有待补字段时状态为 `PRODUCTION_REVIEW_TASKS_REQUIRED`，summary 会按 `ownerLaneCounts`、`ownerLaneBreakdown` 和 `fieldBreakdown` 说明具体缺 `photoEvidenceStatus`、`fieldEvidenceRefs`、`fieldVerifiedBy/fieldVerifiedAt`、`reviewStatus`、`geoStatus`、`auditLicenseStatus`、`status`、`reviewedBy/reviewedAt` 或 `triggerSmokeStatus`；owner-lane CSV 一行一个负责人 lane，包含任务数、POI 数、字段、POI code 和下一步动作，便于把 80 个 POI 生产复核交给现场核验、内容审核、坐标审核和来源授权负责人。需要直接分派给另一个 AI 或人工时，加 `--owner-lane-dir workbench/xicheng-poi-production-review-owner-lanes`，会生成 `workbench/xicheng-poi-production-review-owner-lanes/` 下的分 lane CSV，并在 evidence summary 的 `ownerLaneTaskFiles` 中记录每个文件。该 CSV 只用于分派真实生产审核补齐任务，不会批准 POI，也不是 production evidence。
- 新增 `scripts/build-xicheng-poi-production-manifest-from-workbook.mjs` 和 `npm run xunjing:xicheng:poi:manifest:from-workbook`，用于把审核人填完的 `workbench/xicheng-production-pois.review-workbook.csv` 转成 `workbench/xicheng-production-pois.json`；该脚本只做字段结构转换和根级 `reviewBatch` 写入，并在 manifest 的 `sourceWorkbook.workbookSha256` 记录输入 workbook 的 SHA-256，仍必须继续跑 production manifest gate，不能替代真实来源、授权和现场证据。
- 新增 `scripts/apply-xicheng-poi-production-review-to-workbook.mjs` 和 `npm run xunjing:xicheng:poi:production-review:apply`，用于把审核人填好的逐 POI 现场证据、坐标审核、触发烟测和内容审核结果受控回填到 `workbench/xicheng-production-pois.review-workbook.production-applied.csv`，并输出 `qa/xicheng-poi-production-review-apply-evidence.json`。该工具必须同时校验 `--source-review-apply-evidence qa/xicheng-poi-source-review-apply-evidence.json` 和 `--trigger-smoke-apply-evidence qa/xicheng-poi-trigger-smoke-apply-evidence.json`，只接受非本地 HTTPS/OSS/COS/S3 现场证据引用，且不能用手工填写的 `triggerSmokeStatus=PASSED` 绕过真实 trigger smoke apply evidence；若仍有 POI 未完成复核，状态为 `PRODUCTION_REVIEW_DATA_REMAINS`，不能替代 workbook gate、manifest gate 或 production seed evidence。
- release gate 与最终 `npm run xunjing:xicheng:release:evidence:package` 已二次校验 production review apply evidence 的 trigger smoke apply 摘要；缺 `triggerSmokeApplyEvidenceFile`、`triggerSmokeApplyStatus=TRIGGER_SMOKE_APPLIED`、`triggerSmokeAppliedPoiCount>=80` 或 `triggerSmokePendingPoiCount=0` 的旧版/伪造 evidence 会 fail-closed，release evidence summary 也会记录 `productionReviewTriggerSmokeApply*` 字段。
- 新增 `scripts/verify-xicheng-poi-trigger-smoke.mjs`、`scripts/apply-xicheng-poi-trigger-smoke-to-production-review.mjs`、`npm run xunjing:xicheng:poi:trigger-smoke` 和 `npm run xunjing:xicheng:poi:trigger-smoke:apply`，用于把 80 个 source-applied workbook 点位逐一打到真实 `/app-api/xunjing/triggers/resolve`，生成 `qa/xicheng-poi-trigger-smoke-evidence.json`，再只把通过的点位回写为 `triggerSmokeStatus=PASSED`。这一步只证明触发识别链路，不会自动批准 `photoEvidenceStatus`、`fieldEvidenceRefs`、`reviewStatus`、`geoStatus`、`auditLicenseStatus` 或 `status`。
- 新增 `scripts/verify-xicheng-yudao-trigger-alias.mjs`、`backend/yudao/sql/mysql/xunjing-patch-xicheng-trigger-alias-20260629.sql` 和 `npm run xunjing:yudao:trigger-alias:verify`，用于复验运行库里“烟袋斜街”不再被 `xicheng-shichahai` 别名抢占，且 `xicheng-yandai-xiejie` 保留别名与 OCR 触发词；输出 `YUDAO_XICHENG_TRIGGER_ALIAS_READY` 后再进入整批 trigger smoke。
- 新增 `scripts/verify-xicheng-poi-production-manifest.mjs` 和 `npm run xunjing:xicheng:poi:manifest:gate`，用于真实西城 POI 资料进入 production seed 前的 fail-closed 验收；只有根级批次审核 `manifest-review-batch`、80+ POI、别名、GCJ02 坐标、触发词、HTTPS 来源、授权状态、来源授权证据引用、内容摘要、推荐问题、审核记录和现场照片/触发 smoke 证据全部通过时才输出 `PRODUCTION_POI_MANIFEST_READY`。
- 新增 `scripts/generate-xicheng-poi-production-seed.mjs` 和 `npm run xunjing:xicheng:poi:seed:generate`，用于 manifest gate 通过后生成可审查的 `xicheng-poi-production-seed.sql`；生成 SQL 会写入 `xunjing_poi`、POI 级来源文档、地图点位、现场证据状态和生产 POI 状态报告，未通过 manifest gate 时拒绝生成。生成器也支持 `--evidence-file qa/xicheng-poi-production-seed-generation-evidence.json`，用于记录 manifest 文件/hash、`reviewBatchCode`、批次证据包引用与 SQL 文件/hash；正式发布仍必须再跑 seed verify evidence。
- 新增 `scripts/verify-xicheng-poi-production-seed.mjs` 和 `npm run xunjing:xicheng:poi:seed:verify`，用于 production seed SQL 纳入正式迁移前的 fail-closed 验收；只有 SQL 仍包含 80+ 个 `xicheng-*` POI、全量审核状态、生产 metrics、`review-batch-metrics`、来源授权证据、现场证据状态、POI 级来源文档和已审核公开媒体资产时才输出 `PRODUCTION_POI_SEED_READY`。`licenseEvidenceRef` 和 `fieldEvidenceRefs` 会逐个校验为非本地 HTTPS、`oss://`、`cos://` 或 `s3://` 引用，普通文本、本地文件和 base64 不能冒充生产证据；媒体资产 gate 会校验 `xunjing_media_asset`、`reviewedMediaCount>=8`、`targetMediaAssetCount>=8`、`AUTHORIZED/APPROVED/can_public/can_ai_use`。
- `npm run xunjing:yudao:release:gate` 已新增 `vector-embedding-runtime` 生产门禁；预发/生产候选不能继续使用本地默认的 `SPRING_AI_VECTORSTORE_TYPE=none` 或 `SPRING_AI_MODEL_EMBEDDING=none`，必须启用 Qdrant 和真实 Embedding provider，且 `DASHSCOPE_EMBEDDING_ENABLED=true`、`QDRANT_API_KEY` 已配置。
- `npm run xunjing:yudao:release:gate` 已新增 `xunjing-admin-ui-artifact` 生产门禁；默认读取 `backend/yudao/yudao-ui/yudao-ui-admin-vue3/dist`，也可通过 `--admin-ui-dir` 或 `XUNJING_ADMIN_UI_DIR` 指向服务器实际静态目录。release evidence 会记录 `summary.adminUiDir`、`summary.adminUiIndexHtmlFile`、`summary.adminUiIndexHtmlSha256`、`summary.adminUiJsAssetCount` 和 `summary.adminUiCssAssetCount`；占位页、缺 Vue `#app` 挂载或缺 JS bundle 会 fail-closed。任务导出已把该检查分派到 `admin-ui`。
- `npm run xunjing:yudao:release:gate` 已接入 `release-source-revision`、`--expected-branch feature/xicheng-p0`/`XICHENG_RELEASE_EXPECTED_BRANCH`、`--yudao-baseline-sql`/`YUDAO_BASELINE_SQL`、`--yudao-server-jar`/`YUDAO_SERVER_JAR`、`--yudao-server-build-evidence`/`YUDAO_SERVER_BUILD_EVIDENCE`、`--yudao-server-smoke-evidence`/`YUDAO_SERVER_SMOKE_EVIDENCE`、`--ai-bootstrap-evidence`/`YUDAO_AI_BOOTSTRAP_EVIDENCE`、`--vision-ocr-evidence`/`XICHENG_VISION_OCR_EVIDENCE`、`--object-storage-evidence`/`XICHENG_OBJECT_STORAGE_EVIDENCE`、`--runtime-seed-evidence`/`XICHENG_RUNTIME_SEED_EVIDENCE`、`--production-seed-apply-evidence`/`XICHENG_PRODUCTION_SEED_APPLY_EVIDENCE`、`--poi-workbook-evidence`、`--poi-manifest-evidence`、`--poi-seed-evidence`、`--poi-source-coverage-evidence`/`XICHENG_POI_SOURCE_COVERAGE_EVIDENCE`、`--poi-source-review-apply-evidence`/`XICHENG_POI_SOURCE_REVIEW_APPLY_EVIDENCE` 和 `--poi-production-review-apply-evidence`/`XICHENG_POI_PRODUCTION_REVIEW_APPLY_EVIDENCE`；生产候选必须能读取完整 Yudao baseline、可部署 `yudao-server.jar`、`YUDAO_SERVER_JAR_BUILT`、`XICHENG_YUDAO_SERVER_SMOKE_READY`、`YUDAO_AI_MODEL_BOOTSTRAPPED`、`XICHENG_VISION_OCR_SMOKE_READY`、`XICHENG_OBJECT_STORAGE_SMOKE_READY`、`YUDAO_XICHENG_PRODUCTION_SEED_APPLIED`、`YUDAO_XICHENG_PRODUCTION_SEED_READY`、`SOURCE_COVERAGE_READY`、`SOURCE_REVIEW_APPLIED` 和 `PRODUCTION_REVIEW_APPLIED` 证据。release evidence 会记录 `summary.yudaoServerBuildEvidenceFile`、`summary.yudaoServerBuildMethod`、`summary.yudaoServerBuildGitCommit`、`summary.yudaoServerBuildGitDirty=false`、`summary.yudaoServerBuildJarSha256`、`summary.yudaoServerSmokeEvidenceFile`、`summary.yudaoServerSmokeBaseUrl`、`summary.yudaoServerSmokeTenantId`、`summary.yudaoServerSmokePackageCode`、`summary.yudaoServerSmokePublicReportMapPointCount`、`summary.yudaoServerSmokePublicReportReviewedMediaCount`、`summary.yudaoServerSmokeMediaAssetCount`、`summary.yudaoServerSmokeBuildGitCommit`、`summary.yudaoServerSmokeBuildJarSha256`、`summary.productionSeedApplyEvidenceFile`、`summary.productionSeedApplySeedEvidenceFile`、`summary.productionSeedApplyRuntimeEvidenceFile`、`summary.productionSeedApplyRuntimeSeedStatus`、`summary.runtimeSeedEvidenceFile`、`summary.runtimeSeedProductionReady`、`summary.sourceCoverageEvidenceFile`、`summary.sourceCoverageStatus`、`summary.sourceCoverageUncoveredPoiCount`、`summary.sourceReviewApplyEvidenceFile`、`summary.sourceReviewApplyStatus`、`summary.sourceReviewPendingSourcePoiCount`、`summary.sourceReviewPendingSourcePoiCodes`、`summary.productionReviewApplyEvidenceFile`、`summary.productionReviewApplyStatus`、`summary.productionReviewPendingPoiCount` 和 `summary.productionReviewPendingPoiCodes`，证明同一份 production seed SQL 是通过受控 apply 命令写入目标 Yudao 库后再复验，受控构建 jar 来自同一 `summary.gitCommit`，且部署后的后端 HTTPS 域名能读取西城资源包、公开媒体资产和公益报告汇总，POI 名称来源覆盖、来源授权回填和逐 POI 生产复核回填均已通过。缺 Yudao server build evidence、build evidence 的 `summary.gitCommit` 与 release commit 不一致、缺 Yudao server smoke evidence、缺 production seed apply evidence、缺来源覆盖 evidence、缺两份 POI apply evidence、只生成 seed SQL、只跑 runtime verify、使用旧版 evidence、过旧 evidence 或 hash 不匹配都会 fail-closed。
- `npm run xunjing:yudao:release:gate` 已接入 `--vision-ocr-evidence`/`XICHENG_VISION_OCR_EVIDENCE`；release evidence 会在 `summary.visionOcrEvidenceFile`、`summary.visionOcrModel` 和 `summary.visionOcrProviderSmokeHost` 记录真实 OCR/视觉 provider smoke 证据。缺少该 evidence、证据过旧、不是 `xicheng-vision-ocr-smoke`、未包含 `vision-provider-request`/`vision-provider-smoke`/`secret-redaction`，或证据里出现 key/secret，都会 fail-closed。
- 新增 `scripts/export-xicheng-yudao-release-blocker-tasks.mjs` 和 `npm run xunjing:yudao:release:tasks:export`，用于把 `qa/xicheng-yudao-release-evidence.json` 里的 failed checks 和 blockers 展开为 `workbench/xicheng-yudao-release-blocker-tasks.csv`，也可用 `--poi-output workbench/xicheng-yudao-release-poi-blocker-tasks.csv` 继续展开逐 POI 修复行，并用 `--poi-summary-output workbench/xicheng-yudao-release-poi-summary.csv` 输出一行一个 POI 的汇总视图；普通 CSV 按 `checkName`、`blocker`、`ownerLane`、`taskDetail`、`requiredEvidence`、`verificationCommand`、`taskStatus`、`affectedPoiCount` 和 `affectedPoiCodes` 输出一行一个修复任务，逐 POI CSV 按 `poiTaskKey`、`poiCode`、`checkName`、`blocker`、`ownerLane`、`taskDetail`、`requiredEvidence`、`verificationCommand` 和 `taskStatus` 输出一行一个点位修复任务，POI summary CSV 按 `poiCode` 合并同一点位的 `blockerCount`、`checkNames`、`blockers`、`taskDetails`、`requiredEvidence` 和 `verificationCommands`，适合先按 80 个点位收口，再回到逐 POI CSV 处理细项。owner lane 覆盖 `platform-ops`、`app-ops`、`ai-platform`、`vision-ocr`、`storage-ops`、`yudao-deploy`、`poi-data` 和 `release-manager`。脚本和 `release:preflight` stdout 的 `summary.ownerLaneBreakdown` 会按负责人汇总 `taskCount`、`checkNames` 和 `verificationCommands`，`summary.poiTaskCount` 会给出逐 POI 任务行数，`summary.poiSummaryCount` 会给出唯一受影响 POI 数；同一个 POI 可能因 production review、runtime seed、source license 或 seed apply 多个阻塞出现多行。POI evidence 阻塞会分别指向 workbook gate、manifest gate、seed verify；runtime seed evidence 阻塞会指向 `npm run xunjing:yudao:production-seed:apply`，要求把已审核 production seed 写入目标库并立即生成运行库证据；当 release evidence 里包含 `runtimeSeedGeoReviewRequiredPoiCodes`、`runtimeSeedLicenseReviewRequiredPoiCodes`、`sourceReviewPendingSourcePoiCodes` 或 `productionReviewPendingPoiCodes` 时，POI evidence、production seed、source license、source review apply、production review apply 和 runtime seed 相关任务行都会带出具体待复核 POI code。不能只用 review pack、SQL 文件或单独 runtime verify 代替生产运行库 evidence。该 CSV 只用于拆分真实 HTTPS 域名、微信、AI/OCR、对象存储、完整 baseline、jar、POI evidence、runtime seed evidence 和 Git 修订阻塞，不是 production evidence，也不包含密钥值。
- `npm run xunjing:yudao:release:preflight` 会额外写出一页式协作交接 `workbench/xicheng-yudao-release-handoff.md`，可用 `--handoff-output` 改写输出路径；当 production POI workbook、manifest 或 seed evidence 尚未生成导致逐 POI release 任务为空时，stdout 的 `summary.poiEvidenceBootstrapCommand` 和 handoff 里的 `POI Evidence Bootstrap` 会先给出 `npm run xunjing:xicheng:poi:review:pack` 命令，用于生成 80 行 production review summary 和审核任务。stdout 的 `summary.appReadinessStatus`、`summary.appReadinessCommand` 和 handoff 里的 `APP Readiness Evidence` 会给出西城 APP live evidence 状态与复验命令；缺少 `qa/xicheng-app-readiness-evidence.json` 或 evidence 不是非本地 HTTPS live 检查时，preflight 保持 `NOT_READY`，并在 blocker CSV 中写入 `app-readiness-evidence` / `app-ops` 任务行。该 Markdown 只汇总状态、evidence/CSV 路径、owner lane 和复验命令，不能替代 release evidence 或 production seed evidence。
- 新增 `scripts/verify-xicheng-yudao-runtime-seed.mjs` 和 `npm run xunjing:yudao:runtime-seed:verify`，用于直接查询运行中的 Yudao MySQL；默认本地候选模式通过时输出 `YUDAO_XICHENG_LOCAL_SEED_READY`，summary 包含 `readinessMode`、`packageCode=XICHENG-MAP-001`、`regionCode=beijing-xicheng`、`poiTotal`、`knowledgeDocuments`、`mapPoints`、`localCandidateReady`、`productionReady`、`productionBlockers`、`geoReviewRequiredPoiCodes` 和 `licenseReviewRequiredPoiCodes`，证据文件只能写入 `qa/`、`tmp/` 或 `workbench/`，不会包含 MySQL 密码。预发/生产验收必须传 `--mode production` 或 `--stage production`；此时只要运行库仍有 `REVIEW_REQUIRED` 坐标/授权或缺少 `productionReady=true` 状态报告，就输出 `YUDAO_XICHENG_PRODUCTION_SEED_NOT_READY`，并可从 code 清单定位具体待补 POI；只有生产 seed 真正落库后才输出 `YUDAO_XICHENG_PRODUCTION_SEED_READY`。
- 新增 `scripts/apply-xicheng-yudao-production-seed.mjs` 和 `npm run xunjing:yudao:production-seed:apply`，用于把已通过 `PRODUCTION_POI_SEED_READY` 的西城 production seed SQL 写入目标 Yudao 库，并立即生成 `YUDAO_XICHENG_PRODUCTION_SEED_READY` 运行库证据；命令必须显式传 `--confirm-apply-xicheng-production-seed`，会校验 seed evidence、SQL SHA-256、80+ 已审核 POI、来源授权证据、现场证据和运行库 production metrics，不满足时拒绝写入或输出 `YUDAO_XICHENG_PRODUCTION_SEED_APPLY_NOT_READY`。该命令不会输出 MySQL 密码或 provider key，也不能替代人工 SQL review、真实 POI 审核、真实 production env、AI/OCR/对象存储 smoke 和 release gate。
- 新增 `scripts/build-yudao-server.mjs` 和 `npm run xunjing:yudao:server:build`，用于在新 checkout、CI 或部署机统一构建 `backend/yudao/yudao-server/target/yudao-server.jar`；命令默认 `--builder auto`，会优先执行本机 Maven package，本机 Maven 不存在时自动回退到 Docker Maven 镜像 `maven:3.9.9-eclipse-temurin-17`，也可显式指定 `--builder mvn --mvn /path/to/mvn` 或 `--builder docker --maven-image maven:3.9.9-eclipse-temurin-17`。构建会校验 jar 非空，并可用 `--evidence-file qa/xicheng-yudao-server-build-evidence.json` 记录 `buildMethod`、`gitBranch`、`gitCommit`、`gitDirty=false`、jar 大小和 SHA-256。release gate 和最终 package gate 都会校验该 evidence，并要求其 `summary.gitCommit` 与 release evidence 的 `summary.gitCommit` 一致，jar 路径、大小和 SHA-256 与 `--yudao-server-jar` 或默认 jar 完全一致，不能只提交构建日志、遗留 target jar 或其它提交构建出的 jar。
- 新增 `scripts/stage-yudao-server-release.mjs` 和 `npm run xunjing:yudao:server:stage`，用于把受控构建出的 `yudao-server.jar`、构建证据、`backend/yudao/sql/mysql/` 和 `ops/mysql-init/` 整理为服务器 Compose 可挂载的版本化 release 目录；通过时输出 `YUDAO_SERVER_RELEASE_STAGED`，核心布局必须是 `app/yudao-server.jar`、`sql/mysql/`、`ops/mysql-init/` 和根目录 `xicheng-yudao-server-build-evidence.json`。服务器当前 compose 挂载 `current/app/yudao-server.jar` 与 `current/ops/mysql-init`，后续 AI 或人工发布必须上传 staging 目录内容再切 `current` 软链，不能把 jar 放在 release 根目录。
- 新增 `scripts/verify-xicheng-yudao-server-smoke.mjs` 和 `npm run xunjing:yudao:server:smoke`，用于部署后对 `XUNJING_APP_API_BASE_URL` 生成 Yudao 后端 HTTP smoke evidence；命令会用生产 `tenant-id` 调用 `/app-api/xunjing/resource/package?packageCode=XICHENG-MAP-001` 和 `/app-api/xunjing/public-report/summary?packageCode=XICHENG-MAP-001`，输出 `XICHENG_YUDAO_SERVER_SMOKE_READY`，并记录发布域名、HTTP 状态、资源包 region/status、公益报告 `packageCount`、`reviewedKnowledgeCount` 和 `mapPointCount`。生产发布必须带同批次 `--yudao-server-build-evidence qa/xicheng-yudao-server-build-evidence.json`，server smoke evidence 会记录 `summary.buildGitCommit`、`summary.buildJarSha256` 和 `summary.buildEvidenceFile`；release gate 和最终 package gate 会要求它们与 release evidence 的 `summary.yudaoServerBuildGitCommit`、`summary.yudaoServerBuildJarSha256` 一致。该 evidence 只证明后端服务域名与西城公开数据接口可达，并把 smoke 绑定到受控构建批次，不替代 APP live readiness、AI/OCR/对象存储或 POI 生产审核证据。
- 新增 `scripts/verify-xicheng-release-evidence-package.mjs` 和 `npm run xunjing:xicheng:release:evidence:package`，用于把 Yudao release evidence、Yudao server build evidence、Yudao server smoke evidence、runtime seed evidence、production seed apply evidence、POI workbook evidence、POI manifest evidence、POI seed evidence、POI source coverage evidence、POI source review apply evidence、POI production review apply evidence 和西城 APP 主链 HTTP readiness evidence 合成为最终发布证据包；只有全部通过且未发现明显密钥泄漏时才输出 `XICHENG_RELEASE_EVIDENCE_PACKAGE_READY`。最终证据包会拒绝旧版 POI/release evidence：release evidence 必须包含 `release-source-revision`、`summary.gitCommit`、`summary.gitDirty=false`、`summary.appApiBaseUrl`、`vector-embedding-runtime`、`yudao-server-artifact`、`yudao-server-build-evidence`、`yudao-server-smoke`、`xicheng-runtime-seed-evidence`、`yudaoBaselineSqlFile`、`yudaoBaselineSqlSha256`、`yudaoServerJarFile`、`yudaoServerJarSizeBytes`、`yudaoServerJarSha256`、`summary.yudaoServerBuildEvidenceFile`、`summary.yudaoServerBuildGitCommit`、`summary.yudaoServerBuildJarSha256`、`summary.yudaoServerSmokeEvidenceFile`、`summary.yudaoServerSmokeBaseUrl`、`summary.yudaoServerSmokeTenantId`、`summary.yudaoServerSmokePackageCode=XICHENG-MAP-001`、`summary.yudaoServerSmokePublicReportMapPointCount>=80`、`summary.runtimeSeedEvidenceFile`、`summary.runtimeSeedReadinessMode=production`、`summary.runtimeSeedProductionReady=true`、`summary.runtimeSeedPoiTotal>=80`、`summary.runtimeSeedKnowledgeDocuments>=84`、`summary.runtimeSeedMapPoints>=80`、`summary.sourceCoverageStatus=SOURCE_COVERAGE_READY`、`summary.sourceCoverageUncoveredPoiCount=0`、`summary.sourceReviewApplyStatus=SOURCE_REVIEW_APPLIED`、`summary.sourceReviewPendingSourcePoiCount=0`、`summary.productionReviewApplyStatus=PRODUCTION_REVIEW_APPLIED` 和 `summary.productionReviewPendingPoiCount=0`；package gate 自身还会直接读取 build evidence、server smoke evidence、runtime seed evidence、production seed apply evidence 并执行 `package-source-revision`，在当前 checkout 可用时要求 `summary.packageGitCommit` 与 release evidence 的 `summary.gitCommit` 一致且 worktree 干净，避免旧 release evidence 混入新代码。Yudao server build evidence 必须来自 `artifactType=xicheng-yudao-server-build`、状态为 `YUDAO_SERVER_JAR_BUILT`，且 `summary.gitCommit` 与 release evidence 的 `summary.gitCommit` 一致，jar 路径、大小和 SHA-256 与 release jar 一致；Yudao server smoke evidence 必须来自 `artifactType=xicheng-yudao-server-smoke`、状态为 `XICHENG_YUDAO_SERVER_SMOKE_READY`，且自身 `summary.baseUrl`、tenant、packageCode、HTTP 状态和公益报告计数必须与 release summary 一致，baseUrl 也必须与 APP API 域名一致；workbook evidence 必须包含 `workbookReadyPoiCount>=80`、`workbookPendingPoiCount=0`、`pendingPoiCodes=[]`、`pendingPoiTasks=[]`，manifest evidence 必须包含 `manifest-review-batch`、`regionCode=beijing-xicheng`、`packageCode=XICHENG-MAP-001`、`reviewBatchCode`、`reviewBatchEvidencePackageRef`、`poi-source-license`、`poi-field-evidence`、`manifestFile` 和 `manifestSha256`，seed evidence 必须包含 `seed-preconditions`、`review-batch-metrics`、`regionCode=beijing-xicheng`、`packageCode=XICHENG-MAP-001`、`reviewBatchCode`、`reviewBatchEvidencePackageRef`、`source-license-evidence`、`field-evidence`、`sqlFile` 和 `sqlSha256`，source coverage evidence 必须来自 `artifactType=xicheng-poi-source-coverage` 且包含 `source-review-file`、`source-pages`、`poi-source-coverage`、`summary.coveredPoiCount>=80`、`summary.uncoveredPoiCount=0`，source review apply evidence 必须来自 `artifactType=xicheng-poi-source-review-apply` 且状态为 `SOURCE_REVIEW_APPLIED`，production review apply evidence 必须来自 `artifactType=xicheng-poi-production-review-apply` 且状态为 `PRODUCTION_REVIEW_APPLIED`，APP readiness evidence 必须来自 `artifactType=xunjing-platform-readiness`，且包含 `summary.tenantId`、`summary.baseUrl`、`summary.xichengRegionCode=beijing-xicheng` 和 `summary.xichengPackageCode=XICHENG-MAP-001`；最终包会重新读取 baseline/jar/build evidence/server smoke evidence/runtime seed evidence/production seed apply evidence/manifest/workbook/seed 源文件复算 SHA-256，并在 package summary 里直接展示 workbook 行级完成数、`pendingPoiTasks=[]`、`summary.yudaoServerBuildEvidenceFile`、`summary.yudaoServerBuildGitCommit`、`summary.yudaoServerSmokeEvidenceFile`、`summary.yudaoServerSmokeStatus`、`summary.yudaoServerSmokeBaseUrl`、`summary.runtimeSeedEvidenceFile`、`summary.runtimeSeedStatus`、`summary.productionSeedApplyEvidenceFile`、`summary.productionSeedApplyStatus`、`summary.poiSourceCoverageEvidenceFile`、`summary.poiSourceReviewApplyEvidenceFile`、`summary.poiProductionReviewApplyEvidenceFile`、`sourceCoverageUncoveredPoiCount`、`poiSourceReviewApplyStatus` 和 `poiProductionReviewApplyStatus`，通过 `evidence-consistency` 校验 APP readiness、workbook、manifest、seed、来源覆盖证据和两段 apply evidence 指向同一西城 region/package、同一 POI 审核批次和同一最终 workbook，同时要求 APP readiness `summary.baseUrl` 与 release evidence 的 `summary.appApiBaseUrl` 是同一个非本地 HTTPS 后端域名，并要求 release evidence 记录的 `poiManifestFile`/`poiManifestSha256`、`sourceWorkbookFile`/`sourceWorkbookSha256`、`poiSeedSqlFile`/`poiSeedSqlSha256` 与最终打包输入一致。生产/预发候选必须使用非本地 HTTPS `summary.baseUrl`，本地 HTTP、旧 evidence、hash 不匹配、build evidence 缺失或 Git commit 不一致、server smoke evidence 缺失、来源覆盖未通过、两段 apply 未通过、域名不一致、缺运行库 runtime seed evidence 或批次不一致都会 fail-closed。

## 本轮验证

- 2026-06-28 21:15 `git rev-list --left-right --count HEAD...origin/feature/xicheng-p0` 与 `HEAD...github/feature/xicheng-p0` 均为 `0 0`，本地、Gitee、GitHub 分支一致。
- `docker run --rm -v "$PWD:/repo" -v "$HOME/.m2:/root/.m2" -w /repo/backend/yudao maven:3.9.9-eclipse-temurin-17 mvn -pl yudao-module-xunjing -am -Dtest=XunjingAppServiceImplTest#testAnswerSupportsXichengRagChatSceneWithQuotaCacheRules -Dsurefire.failIfNoSpecifiedTests=false test`：1 个西城 `xicheng-rag-chat` + quota 规则回归测试通过。
- `npm run xunjing:yudao:server:build -- --builder docker --evidence-file qa/xicheng-yudao-server-build-evidence.json`：已构建 `backend/yudao/yudao-server/target/yudao-server.jar`，jar SHA-256 为 `a03b36164ab40d17888fd95213ccc26af88997bc5c0fb485f2cf8bc0cafc2b64`。
- 本地 Docker JRE 重启 `xunjing-yudao-smoke` 后，`POST http://127.0.0.1:48080/app-api/xunjing/ai/chat` 分别用 `sceneCode=xicheng-rag-chat` 与 `sceneCode=xicheng-ai-guide`、`poiCode=xicheng-baitasi` 返回 HTTP 200，`safetyStatus=PASSED`，sources 指向 `妙应寺白塔 POI 级已审核来源`。
- `npm run xunjing:platform:verify -- --env-file ops/xunjing-platform.env --base-url http://127.0.0.1:48080 --tenant-id 1 --skip-admin-check --include-xicheng-app-check --include-xicheng-trigger-check --evidence-file tmp/xunjing-platform-live-xicheng-real-data-current.json`：20/20 通过，包含西城扫码解析、错误反馈、AI 有来源回答、AI 无来源拦截、白塔寺/恭王府/北京天文馆 trigger smoke。
- `npm run xunjing:yudao:release:preflight`：预期 `NOT_READY`，`summary.gitDirty=false` 时才可作为当前 release evidence；阻塞集中在生产 env、真实微信/AI/OCR/对象存储、production seed apply、POI workbook/manifest/seed evidence、生产复核回填、非本地 HTTPS APP readiness evidence 和 80 个 POI 坐标/授权复核。当前 source review apply evidence 已为 `SOURCE_REVIEW_APPLIED` 且 `pendingSourcePoiCount=0`，source review apply 命令不再出现在 `poi-data` lane；production review apply evidence 仍为 `PRODUCTION_REVIEW_DATA_REMAINS`，会把 80 个待现场、坐标、触发烟测和内容审核的 POI 展开进 `summary.poiTaskCount` 和 `workbench/xicheng-yudao-release-poi-blocker-tasks.csv`。若缺少可展开的 POI evidence，`summary.poiEvidenceBootstrapCommand` 和 handoff 的 `POI Evidence Bootstrap` 仍会提示先运行 review pack，生成 workbook、来源 summary、production review summary 和补齐任务。
- `npm run xunjing:xicheng:poi:review:pack -- --evidence-file qa/xicheng-poi-production-review-pack-evidence.json --workbook-evidence qa/xicheng-poi-review-workbook-evidence.json --review-tasks workbench/xicheng-poi-review-tasks.csv --source-review workbench/xicheng-poi-source-review-summary.csv --production-review workbench/xicheng-poi-production-review-summary.csv`：预期 `REVIEW_DATA_REQUIRED`，生成 80 行 review workbook、0 个 ready POI、80 个 pending POI、320 条审核补齐任务、来源分组表和 80 行 production review summary；该输出用于资料补齐，不是 production evidence。`summary.sourceReviewGroupBreakdown` 用于先按官方来源聚合处理授权，`summary.productionReviewRowCount` 用于确认逐 POI 现场、坐标、触发烟测和内容审核待填行数；来源授权前先运行 `npm run xunjing:xicheng:poi:source-coverage:audit`，若输出 `SOURCE_COVERAGE_REVIEW_REQUIRED`，先修正未覆盖 POI 的来源或补充人工证据。通过后可运行 `npm run xunjing:xicheng:poi:source-review:prepare-official -- --source-review workbench/xicheng-poi-source-review-summary.csv --source-coverage-evidence qa/xicheng-poi-source-coverage-evidence.json --output workbench/xicheng-poi-source-review-summary.csv --evidence-file qa/xicheng-poi-official-source-review-evidence.json` 预填官方公开来源授权字段，再运行 `npm run xunjing:xicheng:poi:source-review:apply -- --source-coverage-evidence qa/xicheng-poi-source-coverage-evidence.json ...` 回填逐 POI workbook；脚本会拒绝缺失、非 ready、仍有未覆盖 POI 或不匹配 source review CSV 的来源覆盖证据。
- `npm run test:run`：全量 Vitest 通过。
- `npm run xunjing:platform:verify:static`：通过，包含 `xicheng-seed-data`、`xicheng-poi-seed-quality`、`xicheng-trigger-backend`、`xicheng-ai-source-guard-backend` 与 `xicheng-app-event-backend`。
- 2026-06-29 `npm run xunjing:xicheng:poi:trigger-smoke -- --workbook workbench/xicheng-production-pois.review-workbook.source-applied.csv --base-url http://127.0.0.1:48080 --tenant-id 1 --evidence-file qa/xicheng-poi-trigger-smoke-evidence.json`：真实调用本地 APP API，修正本地旧库 `xicheng-shichahai.aliases_json` 后 80 个 POI 全部通过，evidence 状态为 `XICHENG_POI_TRIGGER_SMOKE_READY`、`passedPoiCount=80`、`failedPoiCount=0`。
- 2026-06-29 `npm run xunjing:xicheng:poi:trigger-smoke:apply -- --production-review workbench/xicheng-poi-production-review-summary.csv --trigger-smoke-evidence qa/xicheng-poi-trigger-smoke-evidence.json --output workbench/xicheng-poi-production-review-summary.csv --evidence-file qa/xicheng-poi-trigger-smoke-apply-evidence.json`：80 个通过项全部回写为 `triggerSmokeStatus=PASSED`，状态 `TRIGGER_SMOKE_APPLIED`、`pendingTriggerSmokePoiCount=0`；该步骤仍不自动批准照片、坐标、内容或来源授权。
- 2026-06-29 本分支已修复 `xicheng-yandai-xiejie` 触发冲突：后端同分排序改为置信度后按距离和 POI 半径收敛，静态 fallback 增加烟袋斜街独立 profile，并从什刹海 seed 别名中移除烟袋斜街；新增 Java 回归用例 `testResolveMultimodalTriggerPrefersPreciseStreetPoiOverNearbyDistrictAlias`。当前机器没有 `mvn` 且仓库没有 `mvnw`，该 Java 单测需在 Maven/Docker Maven 环境复跑。
- 2026-06-29 尝试用 Docker Maven 重建 `yudao-server.jar` 和运行上述 Java 单测；两次 Maven 均长时间停在 Yudao 聚合依赖链，已中止，未得到新的 jar/test 通过证据。当前 80/80 trigger smoke 是基于正在运行的本地 `xunjing-yudao-smoke` 服务和最小 DB 数据修正获得，不等同于新 jar 构建通过。
- 2026-06-29 `MYSQL_CLIENT=container MYSQL_CONTAINER=xunjing-mysql npm run xunjing:yudao:runtime-seed:verify -- --env-file ops/xunjing-platform.env.example --evidence-file qa/xicheng-yudao-runtime-seed-evidence.json`：本地运行库仍为 `YUDAO_XICHENG_LOCAL_SEED_READY`，`poiTotal=80`、`knowledgeDocuments=84`、`mapPoints=80`、`localCandidateReady=true`、`productionReady=false`；阻塞仍是 80 个 POI 坐标复核、80 个 POI 来源授权复核和 public report `productionReady=false`。
- 2026-06-29 `npm run xunjing:platform:verify -- --env-file ops/xunjing-platform.env.example --base-url http://127.0.0.1:48080 --tenant-id 1 --skip-admin-check --include-xicheng-app-check --include-xicheng-trigger-check --evidence-file tmp/xicheng-app-readiness-evidence-local.json`：本地 APP 主链 20/20 通过，包含西城扫码、错误反馈、AI 有来源回答、AI 无来源拦截、白塔寺/恭王府/北京天文馆 trigger smoke。
- 2026-06-29 `npm run xunjing:yudao:release:preflight -- --release-evidence tmp/xicheng-yudao-release-evidence-current.json --tasks-output tmp/xicheng-yudao-release-blocker-tasks-current.csv --poi-tasks-output tmp/xicheng-yudao-release-poi-blocker-tasks-current.csv --poi-summary-output tmp/xicheng-yudao-release-poi-summary-current.csv --handoff-output tmp/xicheng-yudao-release-handoff-current.md`：仍为 `NOT_READY`；trigger smoke 已清零 pending，但 release gate 仍要求真实生产环境、生产 seed apply、生产 runtime seed、POI workbook/manifest/seed evidence、非本地 HTTPS APP readiness evidence、现场证据、坐标审核、内容审核和来源授权。当前数量以 stdout summary 和导出的 CSV 行数为准。
- `npm run xunjing:platform:deps:config`：通过，MySQL 挂载包含 `05-xunjing-seed-xicheng-p0.sql`。
- `JAVA_HOME=tmp/tools/jdk-17.0.19+10/Contents/Home tmp/tools/apache-maven-3.9.10/bin/mvn -pl yudao-module-xunjing -am -Dtest=XunjingAppServiceImplTest,XunjingConsoleServiceImplTest,XunjingVisionRecognitionServiceTest -Dsurefire.failIfNoSpecifiedTests=false test`：43 个后端测试通过。
- `git diff --check`：通过。
- `npm run xunjing:yudao:release:gate -- --stage production --expected-branch feature/xicheng-p0 --env-file ops/xunjing-platform.env.example`：预期输出 `NOT_READY`，`full-yudao-baseline` 已通过并记录 `ruoyi-vue-pro.sql` SHA-256；剩余阻塞项包含真实 HTTPS APP API 域名、微信 AppID、AI/OCR/视觉服务、OCR/视觉 provider smoke evidence、Yudao AI bootstrap evidence、Qdrant collection smoke evidence、Embedding provider smoke evidence、对象存储、manifest/seed evidence、runtime production seed evidence、80/80 西城 POI 和来源授权。
- 2026-06-28 本机 `xunjing-mysql` 已重新执行 `backend/yudao/sql/mysql/xunjing-seed-xicheng-p0.sql`：`XICHENG-MAP-001` 为 `PUBLISHED`，西城 `xunjing_poi` 为 80 条，内容审核 80 条 `APPROVED`，坐标复核 80 条 `REVIEW_REQUIRED`，来源授权 80 条 `REVIEW_REQUIRED`，状态报告为 `p0LocalCandidate=true`、`productionReady=false`。
- `MYSQL_CLIENT=container MYSQL_CONTAINER=xunjing-mysql npm run xunjing:yudao:runtime-seed:verify -- --env-file ops/xunjing-platform.env.example --evidence-file qa/xicheng-yudao-runtime-seed-evidence.json`：用于复验本机 Yudao MySQL 运行库是否真的有西城 seed；通过时状态为 `YUDAO_XICHENG_LOCAL_SEED_READY`，但 `productionReady=false` 仍应保留，直到坐标、授权和现场证据全部补齐。生产/预发运行库证据必须由 `npm run xunjing:yudao:production-seed:apply -- --env-file /secure/path/production.env --seed-sql backend/yudao/sql/mysql/xunjing-seed-xicheng-p0-production.sql --seed-evidence qa/xicheng-poi-production-seed-evidence.json --runtime-evidence-file qa/xicheng-yudao-runtime-seed-production-evidence.json --apply-evidence-file qa/xicheng-yudao-production-seed-apply-evidence.json --confirm-apply-xicheng-production-seed` 写库后生成，期望 runtime evidence 状态为 `YUDAO_XICHENG_PRODUCTION_SEED_READY`。
- 2026-06-28 临时用 Docker JRE 启动 `backend/yudao/yudao-server/target/yudao-server.jar` 验证本地接口在线状态：使用本地候选环境变量 `SPRING_AI_VECTORSTORE_TYPE=none`、`SPRING_AI_MODEL_EMBEDDING=none`、`DASHSCOPE_EMBEDDING_ENABLED=false` 后，服务可连接 MySQL/Redis 并在 `48080` 形成可访问 APP API。
- `curl -H 'tenant-id: 1' 'http://127.0.0.1:48080/app-api/xunjing/resource/package?packageCode=XICHENG-MAP-001'`：返回 `packageCode=XICHENG-MAP-001`、`title=西城 AI 旅伴真实试运营地图`、80 个 map point、84 条 knowledge document，首个点位为 `妙应寺白塔`。
- `curl -H 'tenant-id: 1' -H 'Content-Type: application/json' -d '{...妙应寺白塔...}' http://127.0.0.1:48080/app-api/xunjing/triggers/resolve`：返回 `poiCode=xicheng-baitasi`、`poiName=妙应寺白塔`、`confidence=0.99`、`requiresUserConfirm=false`，来源为西城区公开资料 URL。
- `node scripts/verify-xunjing-platform-readiness.mjs --env-file ops/xunjing-platform.env.example --base-url http://127.0.0.1:48080 --tenant-id 1 --skip-admin-check --include-xicheng-app-check --include-xicheng-trigger-check --evidence-file tmp/xicheng-app-readiness-evidence-local.json`：通过 20/20 项，包含 `live-xicheng-ai-chat-sourced`、`live-xicheng-ai-chat-blocked`、`live-xicheng-trigger-baitasi`、`live-xicheng-trigger-gongwangfu`、`live-xicheng-trigger-planetarium`。

预发或生产 Yudao API 启动后，用以下命令生成西城 APP 主链和 trigger HTTP evidence；`--base-url` 必须是非本地 HTTPS，且必须与同批 release evidence 的 `summary.appApiBaseUrl` 一致：

```bash
node scripts/verify-xunjing-platform-readiness.mjs \
  --env-file /opt/xinghe-xunjing/env/staging.env \
  --base-url https://platform-staging.example.com/ \
  --tenant-id 1 \
  --include-xicheng-app-check \
  --include-xicheng-trigger-check \
  --evidence-file qa/xicheng-app-readiness-evidence.json
```

本地联调只能写入 `qa/xicheng-app-readiness-local-evidence.json` 或 `tmp/xicheng-app-readiness-evidence-local.json`，用于证明 APP 主链功能可用；本地 HTTP evidence 不能作为 final release package 输入。

## APP AI 接口契约

### 识别地点

接口：`POST /app-api/xunjing/triggers/resolve`

请求关键字段：

```json
{
  "packageCode": "XICHENG-MAP-001",
  "regionCode": "beijing-xicheng",
  "sceneCode": "xicheng-multimodal-trigger",
  "sourceChannel": "xicheng-app",
  "userTraceId": "trace-xicheng-device-001",
  "text": "我在白塔寺附近",
  "ocrText": "妙应寺白塔",
  "location": {
    "latitude": 39.9231,
    "longitude": 116.35726,
    "coordType": "GCJ02"
  },
  "imageLabels": ["white_pagoda", "temple"],
  "recentPoiCodes": ["xicheng-baitasi"]
}
```

响应关键字段：

```json
{
  "packageCode": "XICHENG-MAP-001",
  "regionCode": "beijing-xicheng",
  "poiCode": "xicheng-baitasi",
  "poiName": "妙应寺白塔",
  "confidence": 0.85,
  "requiresUserConfirm": false,
  "targetPath": "/pages/ai-guide/detail?regionCode=beijing-xicheng&poiCode=xicheng-baitasi&packageCode=XICHENG-MAP-001",
  "suggestedQuestions": ["白塔寺为什么重要？"],
  "sources": [
    {
      "title": "妙应寺白塔",
      "sourceType": "OFFICIAL_PUBLIC",
      "sourceUrl": "https://www.bjxch.gov.cn/xxgk/zdly/jgxx/lyscjg/"
    }
  ]
}
```

当前注意：

`packageCode` 是触发识别的包级边界。APP 已知当前产品包时必须传 `packageCode=XICHENG-MAP-001`；后台只会在该公开资源包内读取已发布且已审核的 `xunjing_poi` 候选，找不到包内匹配时返回确认/问答兜底，不得把同一区域其它资源包的 POI 拼进当前包的 `targetPath`。

- 触发引擎已优先读取 `xunjing_poi` 表；只有数据库为空或查询异常时才回落到 Java 内置 POI 样例。
- APP 若希望后台自动记录识别事件，必须传 `packageCode=XICHENG-MAP-001`。传入后响应会回显 `packageCode`，并在 `targetPath` 和候选 `targetPath` 中携带 `packageCode`，APP 可将识别结果的 `packageCode`、`regionCode`、`poiCode` 和 `poiName` 直接传给 `/app-api/xunjing/ai/chat`。不传 `packageCode` 时接口仍可识别，但后台不会自动归因并写入 `TRIGGER_RESOLVE` 事件。
- APP 不应上传 `photoMeta.imageBase64` 给事件链路；生产图片上传应走对象存储/上传凭证后只传图片标识或识别服务结果。

### AI 对话

接口：`POST /app-api/xunjing/ai/chat`

请求关键字段：

```json
{
  "packageCode": "XICHENG-MAP-001",
  "regionCode": "beijing-xicheng",
  "poiCode": "xicheng-baitasi",
  "poiName": "妙应寺白塔",
  "question": "白塔寺为什么重要？"
}
```

约束：

- 只允许基于 `review_status=APPROVED` 且 `vector_status=INDEXED` 的知识文档回答。
- 当请求带 `poiCode` 或 `poiName` 时，命中的来源必须匹配该 POI；其他 POI 或泛化西城资料不能作为成功回答来源。
- 无审核来源时返回 `BLOCKED`，不能编造。
- 涉及实时开放、票价、交通、活动时必须提示以官方实时渠道为准。

### 错误反馈

接口：`POST /app-api/xunjing/resource/events`

请求关键字段：

```json
{
  "packageCode": "XICHENG-MAP-001",
  "sceneCode": "xicheng-ai-guide",
  "eventType": "ERROR_FEEDBACK",
  "sourceChannel": "xicheng-app",
  "userTraceId": "trace-xicheng-device-001",
  "payloadJson": "{\"category\":\"ocr_no_match\",\"message\":\"无法识别当前位置\",\"poiCode\":\"xicheng-unknown\",\"severity\":\"WARN\"}"
}
```

当前注意：带 `packageCode` 时，`sceneCode` 可以是普通 APP 场景码；如果不传 `packageCode`，`sceneCode` 仍必须能解析到已启用二维码。

## 剩余上线阻塞

- 真实微信 AppID。
- HTTPS 后端域名。
- OCR / 视觉识别服务和生产 API Key。
- 上传凭证与对象存储。
- 真实 AI Key。
- 生产/预发必须启用 Qdrant 向量检索和 Embedding：`SPRING_AI_VECTORSTORE_TYPE=qdrant`、`SPRING_AI_MODEL_EMBEDDING` 不得为 `none`、`DASHSCOPE_EMBEDDING_ENABLED=true`、`QWEN_EMBEDDING_MODEL` 必须为真实模型，并分别运行 `npm run xunjing:qdrant:smoke -- --env-file /secure/path/production.env --evidence-file qa/xicheng-qdrant-smoke-evidence.json` 和 `npm run xunjing:embedding:smoke -- --env-file /secure/path/production.env --evidence-file qa/xicheng-embedding-smoke-evidence.json` 生成真实 collection 与向量 provider smoke evidence。
- P0 不少于 80 个已复核 POI；后续新增 POI 必须继续通过 `xicheng-poi-seed-quality`，确保每个点位都有别名、坐标、触发词、来源、摘要、推荐问题和审核状态；首批西城后台还必须有 8 张以上 `AUTHORIZED/APPROVED/can_public/can_ai_use` 的公开媒体资产，部署后 server smoke 要同时证明 `/resource/package` 返回 `mediaAssets` 且 public report 的 `reviewedMediaCount>=8`。
- 真实 POI 数据包进入 production seed 前，可先运行 `npm run xunjing:xicheng:poi:review:pack` 生成带 80 个本地候选 POI 的草稿模板、逐点审核清单、可填写字段表、review packet、review pack evidence、workbook gate evidence、来源分组表、production review summary 和待补任务 CSV；模板不会通过 production manifest gate，checklist/workbook/review packet/review pack evidence/workbook evidence/来源分组表/production review summary/任务 CSV 都不能作为 production evidence。需要显式传参时可使用 `npm run xunjing:xicheng:poi:review:pack -- --evidence-file qa/xicheng-poi-production-review-pack-evidence.json --workbook-evidence qa/xicheng-poi-review-workbook-evidence.json --review-tasks workbench/xicheng-poi-review-tasks.csv --source-review workbench/xicheng-poi-source-review-summary.csv --production-review workbench/xicheng-poi-production-review-summary.csv`，或使用底层模板命令：`npm run xunjing:xicheng:poi:manifest:template -- --output workbench/xicheng-production-pois.prefilled.json --seed-sql backend/yudao/sql/mysql/xunjing-seed-xicheng-p0.sql --review-checklist workbench/xicheng-production-pois.review-checklist.csv --review-workbook workbench/xicheng-production-pois.review-workbook.csv --review-packet workbench/xicheng-production-pois.review-packet.json`。审核人可先看 review pack summary 的 `sourceReviewGroupBreakdown` 和 `workbench/xicheng-poi-source-review-summary.csv`，按同一官方来源批量处理授权；授权前运行 `npm run xunjing:xicheng:poi:source-coverage:audit -- --source-review workbench/xicheng-poi-source-review-summary.csv --evidence-file qa/xicheng-poi-source-coverage-evidence.json`，确认每个 POI 名称确实出现在其绑定来源页中。若输出 `SOURCE_COVERAGE_REVIEW_REQUIRED`，先修正未覆盖 POI 的来源或补充人工证据；来源分组 CSV 填好 `licenseStatus=APPROVED`、`licenseEvidenceRef`、`licenseReviewedBy` 和 `licenseReviewedAt` 后，运行 `npm run xunjing:xicheng:poi:source-review:apply -- --workbook workbench/xicheng-production-pois.review-workbook.csv --source-review workbench/xicheng-poi-source-review-summary.csv --source-coverage-evidence qa/xicheng-poi-source-coverage-evidence.json --output workbench/xicheng-production-pois.review-workbook.source-applied.csv --evidence-file qa/xicheng-poi-source-review-apply-evidence.json` 批量回填逐 POI workbook。若缺少 `SOURCE_COVERAGE_READY` evidence、`uncoveredPoiCount` 不为 0 或 evidence 不指向同一 source review CSV，脚本会 fail-closed；若输出 `SOURCE_REVIEW_DATA_REMAINS`，继续补未批准来源组。即使输出 `SOURCE_REVIEW_APPLIED`，也还不是 production evidence，仍需 production review apply、workbook gate、manifest gate 和 seed gate。production review summary 填好 `photoEvidenceStatus=APPROVED`、`triggerSmokeStatus=PASSED`、`reviewStatus/geoStatus/auditLicenseStatus=APPROVED`、`status=PUBLISHED`、非本地 `fieldEvidenceRefs`、`fieldVerifiedBy/fieldVerifiedAt` 和 `reviewedBy/reviewedAt` 后，运行 `npm run xunjing:xicheng:poi:production-review:apply -- --workbook workbench/xicheng-production-pois.review-workbook.source-applied.csv --production-review workbench/xicheng-poi-production-review-summary.csv --source-review-apply-evidence qa/xicheng-poi-source-review-apply-evidence.json --trigger-smoke-apply-evidence qa/xicheng-poi-trigger-smoke-apply-evidence.json --output workbench/xicheng-production-pois.review-workbook.production-applied.csv --evidence-file qa/xicheng-poi-production-review-apply-evidence.json` 回填现场、坐标、触发烟测和内容审核列；该命令会校验 source review apply evidence 已为 `SOURCE_REVIEW_APPLIED`、`pendingSourcePoiCount=0`，且其 `outputFile` 和 `outputSha256` 匹配输入 workbook。审核人补齐真实资料后，先运行 `npm run xunjing:xicheng:poi:manifest:from-workbook -- --workbook workbench/xicheng-production-pois.review-workbook.production-applied.csv --output workbench/xicheng-production-pois.json --production-ready --batch-code xicheng-p0-poi-review-YYYYMMDD --data-owner xicheng-cultural-tourism-review-team --source-compiled-by xicheng-source-compiler --source-compiled-at YYYY-MM-DD --reviewed-by xicheng-production-reviewer --reviewed-at YYYY-MM-DD --evidence-package-ref oss://xunjing-review/xicheng/review-batches/xicheng-p0-poi-review-YYYYMMDD.zip` 生成 manifest，再运行 `npm run xunjing:xicheng:poi:manifest:gate -- --manifest workbench/xicheng-production-pois.json --evidence-file qa/xicheng-poi-manifest-evidence.json`；只有输出 `PRODUCTION_POI_MANIFEST_READY` 且 checks 包含 `manifest-review-batch` 后，才能运行 `npm run xunjing:xicheng:poi:seed:generate -- --manifest workbench/xicheng-production-pois.json --output backend/yudao/sql/mysql/xunjing-seed-xicheng-p0-production.sql --evidence-file qa/xicheng-poi-production-seed-generation-evidence.json` 生成 SQL 和生成证据；生成的 SQL 必须带 `xunjing_assert_xicheng_production_seed_ready` 前置检查，缺少 `XICHENG-MAP-001` 资源包或项目/学校引用时通过 `SIGNAL SQLSTATE '45000'` 中止。随后运行 `npm run xunjing:xicheng:poi:seed:verify -- --sql backend/yudao/sql/mysql/xunjing-seed-xicheng-p0-production.sql --evidence-file qa/xicheng-poi-production-seed-evidence.json` 输出 `PRODUCTION_POI_SEED_READY` 后再人工审查和纳入迁移。
- 每个 P0 POI 的坐标、来源授权、内容审核、来源授权证据引用、现场测试照片证据引用和 `triggerSmokeStatus=PASSED`；根级 `reviewBatch` 必须包含 `batchCode`、`dataOwner`、`sourceCompiledBy/sourceCompiledAt`、`reviewedBy/reviewedAt` 和 `evidencePackageRef`；来源授权证据、现场证据和批次证据包只能使用 `https://`、`oss://`、`cos://` 或 `s3://` 这类非本地引用，不能提交本地文件路径或 base64 图片。
- 预发/生产环境必须验证 `/triggers/resolve` 实际读取数据库 seed，而不是只看本地单元测试。
- 预发环境实机联调和 release evidence。
- `npm run xunjing:yudao:release:preflight -- --stage production --expected-branch feature/xicheng-p0 --env-file /secure/path/production.env --yudao-baseline-sql /secure/path/ruoyi-vue-pro.sql --yudao-server-jar /secure/path/yudao-server.jar --yudao-server-build-evidence qa/xicheng-yudao-server-build-evidence.json --yudao-server-smoke-evidence qa/xicheng-yudao-server-smoke-evidence.json --ai-bootstrap-evidence qa/xicheng-yudao-ai-bootstrap-evidence.json --qdrant-evidence qa/xicheng-qdrant-smoke-evidence.json --embedding-evidence qa/xicheng-embedding-smoke-evidence.json --vision-ocr-evidence qa/xicheng-vision-ocr-smoke-evidence.json --object-storage-evidence qa/xicheng-object-storage-smoke-evidence.json --runtime-seed-evidence qa/xicheng-yudao-runtime-seed-production-evidence.json --production-seed-apply-evidence qa/xicheng-yudao-production-seed-apply-evidence.json --poi-workbook-evidence qa/xicheng-poi-review-workbook-evidence.json --poi-manifest-evidence qa/xicheng-poi-manifest-evidence.json --poi-seed-evidence qa/xicheng-poi-production-seed-evidence.json --poi-source-coverage-evidence qa/xicheng-poi-source-coverage-evidence.json --poi-source-review-apply-evidence qa/xicheng-poi-source-review-apply-evidence.json --poi-production-review-apply-evidence qa/xicheng-poi-production-review-apply-evidence.json --release-evidence qa/xicheng-yudao-release-evidence.json --tasks-output workbench/xicheng-yudao-release-blocker-tasks.csv --poi-tasks-output workbench/xicheng-yudao-release-poi-blocker-tasks.csv --poi-summary-output workbench/xicheng-yudao-release-poi-summary.csv` 会先运行 release gate，再导出阻塞任务 CSV、逐 POI 阻塞任务 CSV 和一行一个 POI 的汇总 CSV；当仍为 `NOT_READY` 时按 `ownerLane`、`taskDetail`、`requiredEvidence` 和 `verificationCommand` 分派阻塞修复，并可先看 `summary.ownerLaneBreakdown` 按负责人确认检查项和复验命令，再用 `summary.poiSummaryCount` 与 `xicheng-yudao-release-poi-summary.csv` 看唯一待补点位，用 `summary.poiTaskCount` 与 `xicheng-yudao-release-poi-blocker-tasks.csv` 逐点补齐坐标、来源授权、来源覆盖、现场证据、source review apply、production review apply 和 production seed。preflight 也会输出 `summary.productionReviewTasksCommand`、`summary.productionReviewTasksStatus`、`summary.productionReviewTaskCount`、`summary.productionReviewPendingPoiCount`、`summary.productionReviewTasksOutputFile`、`summary.productionReviewOwnerLanesOutputFile`、`summary.productionReviewOwnerLaneTaskDir`、`summary.productionReviewOwnerLaneTaskFiles` 和 `summary.productionReviewTasksEvidenceFile`，并默认从 `workbench/xicheng-poi-production-review-summary.csv` 生成字段级 `workbench/xicheng-poi-production-review-tasks.csv`、`workbench/xicheng-poi-production-review-owner-lanes.csv`、`workbench/xicheng-poi-production-review-owner-lanes/*.csv` 与 `qa/xicheng-poi-production-review-tasks-evidence.json`；这些 CSV 只用于分派逐 POI 现场证据、触发烟测、坐标审核、内容审核、授权审核和发布状态补齐任务，不是 production evidence。
- `npm run xunjing:yudao:release:gate -- --stage production --expected-branch feature/xicheng-p0 --env-file /secure/path/production.env --yudao-baseline-sql /secure/path/ruoyi-vue-pro.sql --yudao-server-jar /secure/path/yudao-server.jar --yudao-server-build-evidence qa/xicheng-yudao-server-build-evidence.json --yudao-server-smoke-evidence qa/xicheng-yudao-server-smoke-evidence.json --ai-bootstrap-evidence qa/xicheng-yudao-ai-bootstrap-evidence.json --qdrant-evidence qa/xicheng-qdrant-smoke-evidence.json --embedding-evidence qa/xicheng-embedding-smoke-evidence.json --vision-ocr-evidence qa/xicheng-vision-ocr-smoke-evidence.json --object-storage-evidence qa/xicheng-object-storage-smoke-evidence.json --runtime-seed-evidence qa/xicheng-yudao-runtime-seed-production-evidence.json --production-seed-apply-evidence qa/xicheng-yudao-production-seed-apply-evidence.json --poi-workbook-evidence qa/xicheng-poi-review-workbook-evidence.json --poi-manifest-evidence qa/xicheng-poi-manifest-evidence.json --poi-seed-evidence qa/xicheng-poi-production-seed-evidence.json --poi-source-coverage-evidence qa/xicheng-poi-source-coverage-evidence.json --poi-source-review-apply-evidence qa/xicheng-poi-source-review-apply-evidence.json --poi-production-review-apply-evidence qa/xicheng-poi-production-review-apply-evidence.json --evidence-file qa/xicheng-yudao-release-evidence.json` 必须输出 `PRODUCTION_READY_CANDIDATE`，当前仓库状态仍应输出 `NOT_READY`；`--yudao-server-build-evidence` 必须来自 `npm run xunjing:yudao:server:build`，且 `summary.gitCommit` 与 release evidence 的 `summary.gitCommit` 一致、jar hash 与 release jar 匹配，`--yudao-server-smoke-evidence` 必须来自 `npm run xunjing:yudao:server:smoke -- --yudao-server-build-evidence qa/xicheng-yudao-server-build-evidence.json`，且与 `XUNJING_APP_API_BASE_URL` 域名一致、`summary.buildGitCommit` 与 `summary.yudaoServerBuildGitCommit` 一致、`summary.buildJarSha256` 与 `summary.yudaoServerBuildJarSha256` 一致，`--qdrant-evidence` 必须来自 `npm run xunjing:qdrant:smoke`，`--embedding-evidence` 必须来自 `npm run xunjing:embedding:smoke`，`--runtime-seed-evidence` 和 `--production-seed-apply-evidence` 必须来自同一次 `npm run xunjing:yudao:production-seed:apply`，`--poi-source-coverage-evidence` 必须来自 `npm run xunjing:xicheng:poi:source-coverage:audit` 且 `uncoveredPoiCount=0`，`--poi-source-review-apply-evidence` 必须为 `SOURCE_REVIEW_APPLIED` 且 `pendingSourcePoiCount=0`，`--poi-production-review-apply-evidence` 必须为 `PRODUCTION_REVIEW_APPLIED` 且 `pendingProductionReviewPoiCount=0`，分别证明目标后端 HTTP 服务、目标 Qdrant collection、目标 Embedding provider、目标运行库复验、production seed 受控写库、POI 来源页覆盖、来源授权回填和逐 POI 生产复核都已通过。若输出 `NOT_READY`，也可运行 `npm run xunjing:yudao:release:tasks:export -- --release-evidence qa/xicheng-yudao-release-evidence.json --output workbench/xicheng-yudao-release-blocker-tasks.csv --poi-output workbench/xicheng-yudao-release-poi-blocker-tasks.csv --poi-summary-output workbench/xicheng-yudao-release-poi-summary.csv` 单独重新导出阻塞任务。
- 西城 APP 主链 live evidence 必须通过 `node scripts/verify-xunjing-platform-readiness.mjs --env-file /secure/path/production.env --base-url https://your-production-domain/ --tenant-id 1 --skip-admin-check --include-xicheng-app-check --include-xicheng-trigger-check --evidence-file qa/xicheng-app-readiness-evidence.json` 生成；`--evidence-file` 只能写入 `qa/`、`tmp/` 或 `workbench/`，不能用 stdout 重定向到仓库根目录或密钥目录。
- 最终发布审核前必须运行 `npm run xunjing:xicheng:release:evidence:package -- --stage production --release-evidence qa/xicheng-yudao-release-evidence.json --yudao-server-build-evidence qa/xicheng-yudao-server-build-evidence.json --yudao-server-smoke-evidence qa/xicheng-yudao-server-smoke-evidence.json --runtime-seed-evidence qa/xicheng-yudao-runtime-seed-production-evidence.json --production-seed-apply-evidence qa/xicheng-yudao-production-seed-apply-evidence.json --poi-workbook-evidence qa/xicheng-poi-review-workbook-evidence.json --poi-manifest-evidence qa/xicheng-poi-manifest-evidence.json --poi-seed-evidence qa/xicheng-poi-production-seed-evidence.json --poi-source-coverage-evidence qa/xicheng-poi-source-coverage-evidence.json --poi-source-review-apply-evidence qa/xicheng-poi-source-review-apply-evidence.json --poi-production-review-apply-evidence qa/xicheng-poi-production-review-apply-evidence.json --app-readiness-evidence qa/xicheng-app-readiness-evidence.json --evidence-file qa/xicheng-release-evidence-package.json` 并输出 `XICHENG_RELEASE_EVIDENCE_PACKAGE_READY`；最终证据包会直接复算 Yudao baseline、Yudao server jar、Yudao server build evidence、Yudao server smoke evidence、runtime seed evidence、production seed apply evidence、POI workbook、POI manifest 和 seed 源文件哈希，并校验 `evidence-consistency`，release evidence 记录的 build evidence、server smoke evidence、POI 源文件路径、hash、来源覆盖 evidence 路径和两份 POI apply evidence 路径必须与 package 输入一致，APP readiness evidence 的 `summary.baseUrl` 也必须与 release evidence 的 `summary.appApiBaseUrl` 一致，release evidence 还必须带 `summary.yudaoServerBuildEvidenceFile`、`summary.yudaoServerBuildJarSha256`、`summary.yudaoServerSmokeEvidenceFile`、`summary.yudaoServerSmokeBaseUrl`、`summary.yudaoServerSmokePackageCode=XICHENG-MAP-001`、`summary.yudaoServerSmokePublicReportMapPointCount>=80`、`summary.yudaoServerSmokeBuildGitCommit=summary.yudaoServerBuildGitCommit`、`summary.yudaoServerSmokeBuildJarSha256=summary.yudaoServerBuildJarSha256`、`summary.aiBootstrapEvidenceFile`、`summary.embeddingEvidenceFile`、`summary.embeddingProviderSmokeModel`、`summary.embeddingVectorDimensions`、`summary.visionOcrEvidenceFile`、`summary.objectStorageEvidenceFile`、`summary.runtimeSeedEvidenceFile`、`summary.productionSeedApplyEvidenceFile`、`summary.productionSeedApplyRuntimeSeedStatus=YUDAO_XICHENG_PRODUCTION_SEED_READY`、`summary.runtimeSeedProductionReady=true`、`summary.sourceCoverageStatus=SOURCE_COVERAGE_READY`、`summary.sourceCoverageUncoveredPoiCount=0`、`summary.sourceReviewApplyStatus=SOURCE_REVIEW_APPLIED`、`summary.sourceReviewPendingSourcePoiCount=0`、`summary.productionReviewApplyStatus=PRODUCTION_REVIEW_APPLIED`、`summary.productionReviewPendingPoiCount=0` 和对象存储写读删状态摘要，不能只提交孤立 JSON 摘要、旧 hash 摘要、不同后端域名、未通过受控 build 的 jar、缺少部署后 server smoke、未通过受控 apply 写入运行库的 seed、未通过来源覆盖审计、未通过两段 POI apply、未通过 Embedding smoke 或混用不同批次 evidence。
- 最终证据包还会要求 manifest evidence、seed evidence 和 Yudao server smoke evidence 都包含 `media-assets` 检查，并要求 release/server smoke summary 同时证明 `yudaoServerSmokeMediaAssetCount>=8`、`yudaoServerSmokePublicReportReviewedMediaCount>=8`；只写入 POI 文本和地图点、但没有可公开图片资产，不能判定为西城后台真实填充完成。

## 下一步最小任务

先用 `npm run xunjing:xicheng:poi:review:pack` 生成带 80 个本地候选 POI 的清单草稿、审核清单、可填写字段表、review packet、`qa/xicheng-poi-production-review-pack-evidence.json`、`qa/xicheng-poi-review-workbook-evidence.json`、`workbench/xicheng-poi-source-review-summary.csv`、`workbench/xicheng-poi-production-review-summary.csv` 和 `workbench/xicheng-poi-review-tasks.csv`；先按来源分组表运行 `npm run xunjing:xicheng:poi:source-coverage:audit -- --source-review workbench/xicheng-poi-source-review-summary.csv --evidence-file qa/xicheng-poi-source-coverage-evidence.json` 确认来源页覆盖 POI，再运行 `npm run xunjing:xicheng:poi:source-review:prepare-official -- --source-review workbench/xicheng-poi-source-review-summary.csv --source-coverage-evidence qa/xicheng-poi-source-coverage-evidence.json --output workbench/xicheng-poi-source-review-summary.csv --evidence-file qa/xicheng-poi-official-source-review-evidence.json` 预填官方公开来源授权字段，用 `npm run xunjing:xicheng:poi:source-review:apply -- --source-coverage-evidence qa/xicheng-poi-source-coverage-evidence.json` 批量回填来源授权列；再运行 `npm run xunjing:xicheng:poi:trigger-smoke -- --workbook workbench/xicheng-production-pois.review-workbook.source-applied.csv --base-url https://your-production-domain/ --tenant-id 1 --evidence-file qa/xicheng-poi-trigger-smoke-evidence.json` 和 `npm run xunjing:xicheng:poi:trigger-smoke:apply -- --production-review workbench/xicheng-poi-production-review-summary.csv --trigger-smoke-evidence qa/xicheng-poi-trigger-smoke-evidence.json --output workbench/xicheng-poi-production-review-summary.csv --evidence-file qa/xicheng-poi-trigger-smoke-apply-evidence.json` 回填触发烟测状态；随后用 `npm run xunjing:xicheng:poi:production-review:apply -- --source-review-apply-evidence qa/xicheng-poi-source-review-apply-evidence.json --trigger-smoke-apply-evidence qa/xicheng-poi-trigger-smoke-apply-evidence.json` 回填现场、坐标、触发烟测和内容审核列，再运行 `npm run xunjing:xicheng:poi:workbook:gate -- --workbook workbench/xicheng-production-pois.review-workbook.production-applied.csv --evidence-file qa/xicheng-poi-review-workbook-evidence.json` 校验最终 workbook，随后依次跑 manifest gate、seed generate 和 seed verify，确认 `backend/yudao/sql/mysql/xunjing-seed-xicheng-p0-production.sql` 输出 `PRODUCTION_POI_SEED_READY`。生产 seed 写入目标 Yudao 库必须运行 `npm run xunjing:yudao:production-seed:apply -- --env-file /secure/path/production.env --seed-sql backend/yudao/sql/mysql/xunjing-seed-xicheng-p0-production.sql --seed-evidence qa/xicheng-poi-production-seed-evidence.json --runtime-evidence-file qa/xicheng-yudao-runtime-seed-production-evidence.json --apply-evidence-file qa/xicheng-yudao-production-seed-apply-evidence.json --confirm-apply-xicheng-production-seed`，确认 apply evidence 输出 `YUDAO_XICHENG_PRODUCTION_SEED_APPLIED` 且 runtime evidence 输出 `YUDAO_XICHENG_PRODUCTION_SEED_READY`。随后在 clean `feature/xicheng-p0` 上运行 `npm run xunjing:yudao:server:build -- --builder docker --evidence-file qa/xicheng-yudao-server-build-evidence.json`，确认 build evidence 的 `summary.gitCommit` 等于当前 release commit，再把 `--yudao-baseline-sql`、`--yudao-server-jar`、`--yudao-server-build-evidence qa/xicheng-yudao-server-build-evidence.json`、`--yudao-server-smoke-evidence qa/xicheng-yudao-server-smoke-evidence.json`、`--ai-bootstrap-evidence`、`--vision-ocr-evidence`、`--object-storage-evidence`、`--runtime-seed-evidence`、`--production-seed-apply-evidence`、workbook/manifest/seed 三份 evidence、`--poi-source-coverage-evidence qa/xicheng-poi-source-coverage-evidence.json`、`--poi-source-review-apply-evidence qa/xicheng-poi-source-review-apply-evidence.json` 和 `--poi-production-review-apply-evidence qa/xicheng-poi-production-review-apply-evidence.json` 传给 `npm run xunjing:yudao:release:gate` 生成 `qa/xicheng-yudao-release-evidence.json`。若 release gate 仍是 `NOT_READY`，立即运行 `npm run xunjing:yudao:release:tasks:export -- --release-evidence qa/xicheng-yudao-release-evidence.json --output workbench/xicheng-yudao-release-blocker-tasks.csv --poi-output workbench/xicheng-yudao-release-poi-blocker-tasks.csv`，把环境、微信、AI/OCR、对象存储、AI bootstrap、OCR smoke、baseline、jar、server build evidence、server smoke evidence、POI evidence、来源覆盖 evidence、两段 POI apply evidence、seed apply evidence、runtime seed evidence 和 Git 阻塞按 owner lane 拆单，并把 POI 数据阻塞展开为逐点任务。只有 Yudao server build evidence、Yudao server smoke evidence、Yudao production seed apply evidence、runtime seed evidence、来源覆盖 evidence、来源授权回填 evidence 和生产复核回填 evidence 同时证明 release jar 来自当前 release commit 的受控构建、部署后 HTTP 服务可读取西城公开数据、该 SQL 已通过受控命令进入目标运行库、POI 来源页覆盖已通过、来源授权与逐 POI 生产复核已回填到最终 workbook，才能说“后台真实填充完成”。通过后启动本地 compose/Yudao 服务或预发环境，执行 `--include-xicheng-app-check --include-xicheng-trigger-check --evidence-file qa/xicheng-app-readiness-evidence.json`，最后运行 `npm run xunjing:xicheng:release:evidence:package` 生成最终发布证据包。
